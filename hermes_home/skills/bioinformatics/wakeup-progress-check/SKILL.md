---
name: wakeup-progress-check
description: 系统唤醒/进度询问时的任务状态核查规程。定位活跃 task_plan → 终态验证 → 三源交叉验证 → 汇报。触发："[系统唤醒]"、"还在跑吗"、"进度"、cron 唤醒、跨会话恢复。
---

# Wakeup Progress Check — 唤醒/进度核查规程

## 何时触发

- 🔴 **用户抱怨"为什么一直在唤醒" / "把唤醒停掉" → 停止 LoopX 唤醒（2026-08-12 用户确认选项 A 实测）**：根因 = `results/<sid>/.loopx/registry.json` 中 goal status=active → LoopX 每分钟 tick 发唤醒。修复 = status 改 `paused` + `quota.compute` 改 `0`（⚠️ 只改 cleared 不够，waiting 会被 bridge 放行）；验证 = `LoopXBridge.should_run()` 返回 should_run=False。完整步骤（含备份/恢复/用户偏好教训）→ `references/stopping-loopx-wakeups.md`
- 消息包含 `[系统唤醒]` / `⏰` / cron 唤醒
- 消息头含 `📊 LoopX 状态：goal: ... | attention: ... | todos: ...`（LoopX 心跳唤醒变体，2026-08-09 实测；其后的 `[系统唤醒 #N]` 编号是 LoopX 计数器，**不作 task_plan 记录编号**——按已有记录最大号+1 或按终态不追加规则处理）
- 🔴 **LoopX 消息模板陷阱（#96 实测 2026-08-09）**：消息正文三条（读 task_plan → search_files → 继续执行）只是**触发信号，不是完整流程**——它不含本 skill 加载步骤；照模板字面执行必跳 skill 门禁 → 进程源滑回被禁 `//FO CSV` 变体（缺口 #14 实证）。Step 0 无条件 `skill_view(wakeup-progress-check)`，三源只用 verify_wakeup.sh 或 Step 3 快查三条。重复违规台账与不可信记录黑名单 → `references/recurring-execution-gaps.md`
- 用户问"还在跑吗" / "进度怎么样" / "跑到哪了"
- 新会话启动但 memory 显示有历史任务

## 核心原则

**查完再答，不查就答 = 撒谎（铁律 -2）。** 三个独立数据源交叉验证一致后才能汇报状态。

## Step 1: 定位活跃 task_plan（最新 mtime 优先）

```bash
ls -lt results/*/task_plan.md | head -5
```

- 💡 **替代命令：`search_files(target='files', pattern='task_plan.md', path='<results根>')`（唤醒 #74 实测 2026-08-09）**：返回值按 mtime 降序（最新在前），一次列出全部 task_plan（本会话 4 个：memomics-1c1890da 最新 → 依次），避开 MSYS `ls -lt` 通配/路径坑；同样以第一条 = 最新 mtime 为准选当前主线。
- ✅ **显式 E:/ 绝对路径的 `ls -lt` 本机可靠（2026-08-09 skip 唤醒实测）**：`ls -lt MEMOMICS_HOME/results/*/task_plan.md 2>/dev/null | head -20` 一次成功列出全部 4 个 task_plan 并按 mtime 降序（memomics-1c1890da 13:20 最新 → 依次）——MSYS 通配/路径坑主要在**相对路径**（`results/*/task_plan.md` 无驱动器前缀）；用显式 `E:/` 驱动器前缀 + 通配符时 `ls -lt` 可直接用，不必绕 search_files。

- 多个 task_plan 并存时**取最新 mtime** 作为当前主线。
- ⛔ 不要假设唤醒消息/记忆里的目录就是活跃任务——以 mtime 为准。
- ⛔ 不要抄旧编号从 0 重计：终态任务（completed/等用户指示）不重启。
- **唤醒记录编号以 task_plan 实际已有为准，消息头 #N 不作数（2026-08-09 实测）**：消息标"[系统唤醒 #1]"但
  task_plan 已含"唤醒 #1 完成 P3-P6"+"唤醒 #2 终态确认" → 本次实际是 #3。取最大号：read_file 全文人工数
  （唤醒 #10 实测：读到 #1-#9 → 追加 #10），或 `grep -oE '唤醒 #[0-9]+' task_plan.md | grep -oE '[0-9]+' | sort -n | tail -1`
  再 +1。✅ **`grep -oE` 取最大号 = 本机可靠路径（#63 二次实证 2026-08-09）**：`grep -oE "唤醒 #([0-9]+)" task_plan.md | sort -t'#' -k2 -n | tail -1` 实测返回真实最大号（#60/#61/#62 → 本次追加 #63），与 #52 的 `grep -c` 假阴性、#61/#63 的 search_files 假阴性**不冲突**——编码假阴性限 `grep -c`（行计数）与 ripgrep search_files（内容搜索）；`grep -oE`（-o 提取模式 + 裸模式、不带 `^##`/`🏁` emoji 锚）正常可用。取号用 grep -oE 一条命令，数逐字相似块仍用 read_file（#52 立规不变）。
  🔴 **grep -oE 取号被合并块范围标题欺骗（唤醒 #84 实测 2026-08-09）**：合并块标题若用范围格式 `唤醒 #NN-#MM 记录`（如 `## 🏁 唤醒 #80-#83 记录`），`grep -oE "唤醒 #([0-9]+)"` 只捕获 `唤醒 #80`（模式停在连字符）→ 返回 #80 但真实最大写盘号是 #83（#81/#82/#83 在标题范围区间与 body 引用 `#80/#81 有 2 node` 中，不直接跟在 `唤醒 ` 后，-o 不捕获）。⚠️ **取号后必须 read_file 核对最后一个记录块标题**：标题含 `-#MM` 区间 → 最大号 = MM 而非 grep 返回的 NN。本次 #84 汇报按 #83+1 计；跳过追加不写盘 → 下次仍按最大写盘号+1（#81 立规不变）。⛔ **不要用 `grep -c '唤醒 #'` 取号** —— 它数的是"包含该子串的行数"，body 里"与唤醒 #2/#4/#5 一致"
  这类行也计入 → 计数远超最大号（10 条记录时计数 15+），会跳号；⛔ 直接抄消息头编号会撞号/从 0 重计。
🔴 **取号新陷阱：合并块内裸 bullet `- #NN（时间）` 对 grep 不可见（唤醒 #90 实测 2026-08-09）**：#88/#89 用扩块法直接以 bullet 追加进 `## 🏁 唤醒 #80-#88 记录` 块内（无 `## 🏁 唤醒 #NN 记录` 标题、无 `唤醒 #NN` 前缀）→ `grep -oE "唤醒 #([0-9]+)"` 只返回块标题起点 #80，真实最大写盘号 #89 被漏（本唤醒 grep 实测 60/73/80，read_file 全文看到 line 141 `- #89（12:0x）` 才正确定 #90）。⚠️ **取号必须 read_file 读到合并块内最后的裸 bullet**：判最大号 = max(标题范围 MM, 块内最后 bullet NN)，不能只 grep 标题。
💡 **裸 bullet 取号专用变体（唤醒 #106 实测 2026-08-09）**：块内裸 bullet 无 `唤醒 #` 前缀时，`grep -oE '#[0-9]+' <task_plan> | sort -t'#' -k2 -n | tail -3` 可抓全部 #NN（含裸 bullet、含标题范围）——比 `唤醒 #([0-9]+)` 更宽容，直接绕过 #90 的裸 bullet 不可见问题（#106 实测返回 #104/#105/#105 → 最大 #105 正确，与 read_file 一致）。⚠️ 代价：正文任何 #数字（body 引用、序号）都被抓，可能误取大号 → 取号后仍必须 read_file 核对最后记录块（#84 规则不变），或用 `sort -k2 -n | tail -3` 看前三名人工排除非记录号。⛔ search_files 内容搜索对中文标题假阴性第 N 次实证（`pattern='唤醒 #\d+'` 与 `'#\d+[（(]'` 均返回 0）——取号一律 terminal grep，search_files 不可作"记录不存在"证据（#61/#63 立规不变）。🔴 **扩块法必须同步更新块标题范围（#90 立规）**：块内追加 bullet 时同时把标题改为 `#80-#90 记录`——本唤醒追加 #90 后标题仍为 #80-#88（未同步），下次唤醒以块内 bullet 为准；标题滞后既坑 grep 取号又破坏"最后记录块标题"锚点。⚠️ 本唤醒进程源又滑回被禁 `tasklist //FI "IMAGENAME eq X.exe" 2>/dev/null` 三连（#76/#82 纪律后第 3 次）→ 空输出靠 GPU+磁盘两源兜住，记录 #90 进程源不可信；下次唤醒第一步必须 skill_view 本 skill 再跑 verify_wakeup.sh。
- **终态记录已存在 + 磁盘无新产出 → 不追加重复记录（2026-08-09 实测）**：判断"是否要写新唤醒记录/是否要继续"，
  先把产出文件最新 mtime 与 task_plan 上次唤醒记录时间戳对比（`ls -lt <工作目录>/*.{csv,bed,md,rds,png} | head` 取最新一行）。
  若上次终态记录已写、且**没有任何文件 mtime 晚于该记录** → 本次=纯汇报，不追加重复终态记录，保持现状等用户。
  - ⚠️ **系统内部文件不算"新产出"（2026-08-09 实测）**：`ls -lt <会话目录>` 全目录比较时，`token_usage.jsonl` /
    `system_log.jsonl` / monitor 日志会持续写入，mtime 可能晚于终态记录（实测 token_usage.jsonl 02:29 vs plan 02:20），
    但它们是系统 token 计数/日志，**不是分析产出**。判"无新产出"一律用扩展名过滤的
    `ls -lt <目录>/*.{csv,bed,md,rds,png}` 或显式排除 jsonl/log，别被 token_usage.jsonl 误导追加记录/误报活跃。
  - 💡 **`find -newermt` 一行判"无新产出"（唤醒 #6 实测 2026-08-09）**：`ls -lt` 过滤法的省事替代——
    `find <会话目录> -newermt "<上次终态记录时间戳>" -type f | grep -vE '\.loopx|token_usage|system_log|heartbeat|monitor|\.log'`
    直接列出"晚于终态记录的非系统文件"，空 = 纯汇报不追加。⚠️ **`.loopx/` 也算系统内部文件**（goal/runs
    心跳 JSON+MD 持续写入，唤醒 #6 实测：晚于 02:30 的只有 .loopx runs + task_plan 自身 + jsonl → 正确判
    终态且未追加重复记录）；判"无新产出"时排除项必须含 `.loopx`，否则误报活跃。
  - 🔴 **基线 = task_plan.md 自身 mtime，不是上次记录时间戳（唤醒 #7 实测 2026-08-09）**：上一唤醒写终态记录时
    "磁盘无新产出"可能已过期——**记录之后又生成了新文件**（实测 `patent/CLUSTER_STEP_BY_STEP_GUIDE.md` 02:53 vs
    task_plan 02:43，晚 10 min，唤醒 #6 漏记）。判"有无未记账产出"一律用
    `find <会话目录> -newermt "$(ls -l <dir>/task_plan.md | awk '{print $6,$7,$8}')" -type f | grep -vE '\.loopx|token_usage|system_log|\.log'`
    （或直接手填 task_plan mtime），列出的候选**逐个确认是否已记账**——这比只看上次记录时间戳多抓"记录后新生成"的一批。⚠️ **手填 task_plan mtime 作基线时，find 会把 task_plan.md 自身列进结果（唤醒 #39 实测：基线 05:03 → 仅命中自身 → 无新产出）**——自身出现是预期行为，不算未记账产出；判"无新产出"时排除自身即可，别因 find 命中自身而误报活跃/补账。
  - 💡 **发现晚于 task_plan 的新文件 = 补账，不是重启（唤醒 #7 实测 2026-08-09）**：若文件属于已标记 completed
    Phase 的产出（如集群正式版执行手册）→ 先验证结构完整（如 `grep -n '^# M'` 见 M1-M9 全在）→ **追加一条新唤醒
    记录补账**（文件路径/时间/大小/内容摘要 + 上一唤醒为何漏：记录先于文件生成）→ 不重跑、不标 in_progress、不误报活跃。
  - 🔴 **补账判定 = 磁盘 mtime 优先于"谁写的"（2026-08-12 唤醒 #110 实测）**：find 命中 `patent_test/00_read40_filter.R`（mtime 08-11 02:00，晚于 task_plan 08-09 15:29 两天）→ 即使 system_log 显示是**本 session 自己** write_file 的（02:00:44 写入 + 语法验证），task_plan 无任何对应记录 = **未记账产出**，必须补账。流程：① 记下文件 mtime + 大小 + 内容摘要（读脚本头注释判断用途/阶段）② grep 本 session system_log 找回 write_file 时刻 + 后续验证记录（确认写了什么、验证了什么）③ 确认"未运行"（脚本是给集群执行的，无输出产物）④ 追加补账唤醒记录（文件路径/时间/内容/未运行状态/红线不变）。⚠️ **不要因为"文件是本 session 写的"就以为已记账**——write_file 不等于 task_plan 记录，mtime 晚于 plan 就是未记账。
- 🔧 **追加唤醒记录时的 patch 锚点防歧义（唤醒 #17 实测 2026-08-09，memomics-1c1890da）**：终态任务的记录尾部常是同一句
  重复行（如 `- 无新增待办，等待用户指示正式版（猴 ≥6 个体×≥3 年龄组 + 人全量 40 样本，红线=不自动启动）`，在 #2-#17
  每段记录里都出现）——直接拿它当 `old_string` patch 会因不唯一报错。解法：**把后面的唯一标题一起包进 old_string**
  （如该重复行 + `\n\n## 红线与约束`），new_string 在中间插入新记录并保留标题。⛔ 不要用 replace_all=true 碰这类
  重复行（会把每段记录尾部全替换）。
- 🔴 **追加前必做清单（#70 实测 2026-08-09 立规，执行缺口第 10 次）**：追加唤醒记录前按序三步——① `read_file` 数
  task_plan 里 `## 🏁 唤醒 #N 记录` 标题行数（勿用 grep -c，中文标题假阴性）；② 若 ≥5 → **先合并旧 N-1 条为一条
  （保留最新一条细节）再追加**（execute_code 切片法或 patch 整块替换）；③ 用「最后记录标题行起锚的完整记录块 +
  `\n\n## 红线与约束`」追加。三步同一回合完成，写完记录不算完。⛔ #70 实测：task_plan 已有 #60-#69 九条逐字相似
  终态记录（远超 ≥5）仍未先合并就追加 #70 = 缺口第 10 次——下次唤醒 #71 追加前必须合并 #60-#70 为一条。
- 🔴 **锚点「尾部重复行 + ## 红线与约束」在高同质记录下失效（#70 实测 2026-08-09）**：#60-#69 记录高度同质
  （磁盘基线时间戳同为 2026-08-09 08:33、待办行逐字相同）时，用「磁盘 find 行 + 无新增待办行 + `\n\n## 红线与约束`」
  作 old_string 报 **Found 10 matches**——patch 模糊匹配（9 策略）在前导行逐字重复时即使尾部有唯一标题仍判多匹配。
  修复（#70 实证一次成功）：**锚点改为「最后记录标题行（`## 🏁 唤醒 #NN 记录（...）`）开头的完整记录块 +
  `\n\n## 红线与约束`」**——从记录头部唯一标题起锚，不再依赖尾部唯一标题兜底。锚点法则升级：#17-#67 的
  「尾部重复行 + 唯一标题」在记录同质化后不可靠，一律改为「记录头部标题行 + 完整正文 + 红线标题」。
- 🔧 **patch 转义陷阱：old_string/new_string 里的转义引号触发 Escape-drift 拒绝（唤醒 #72 实测 2026-08-09）**：从 read_file 输出复制记录文本进 patch 参数时，若把记录里的引号写成反斜杠转义形式（如 `find -newermt \"2026-08-09 09:00\"` 复制成了带 `\\\"` 的文本），patch 报 `Escape-drift detected: ... literal sequence ... but the matched region ... does not` 直接拒绝（0 修改，文件不变）。修复：patch 的 old_string/new_string 一律用裸引号（不转义），并优先选短唯一锚点（如 `## 红线与约束` 标题行 + 少量上下文）而不是整段复制——整段复制最容易引入转义残留。实测：第一次带转义引号失败，第二次裸引号 + 短锚点一次成功。
- ✅ **合并大块尾锚点：裸节标题单行即可（唤醒 #108 实测 2026-08-09）**：当最后记录是已合并的大块（如 `## 🏁 唤醒 #80-#107 记录` 整块数十行，正文不适合做 old_string）时，不必走 #70 的「记录头部标题 + 完整正文」长锚点——old_string 直接用唯一的尾部节标题 `## 红线与约束`（本 task_plan 全文件仅出现一次）即可，new_string = 新记录 + `\n\n## 红线与约束`，一次成功、零歧义。适用前提：目标节标题在文件中唯一（红线/Errors/Decisions 标题均可作候选）；若节标题也重复（少见）回退 #70 头部锚。
⚠️ **EOF 后插会拆散记录链分组（唤醒 #115 实测 2026-08-12）**：memomics-1c1890da 的节顺序 = 唤醒记录块 → `## 红线与约束` → `## Errors Encountered` → `## Decisions Made`（红线在记录块之后、Errors/Decisions 在红线之后）。用 Errors/Decisions 段最后一行作锚点**后插** → 新记录落到文件最末（Decisions 之后）；而 canonical 锚点 `## 红线与约束` 是**前插**（新记录落在红线标题前）→ 下次唤醒按 canonical 追加时，记录链被 #115 拆成两段（红线前一段 + 红线后/Decisions 后一段），"最后记录块标题"锚点失效。规则：**统一用红线标题前插（old_string=`## 红线与约束`，new_string=新记录+红线标题）**；Errors/Decisions 标题只能作"唯一性候选"不能作插入锚（会插到红线之后，同样拆链）；若上一唤醒已 EOF 后插 → 下次读 plan 时先把它挪回红线前再继续。
- 📝 **终态纯汇报也追加一行简短记录（实际实践，与上方"不追加重复记录"不矛盾）**："不追加"指不重写完整终态长总结
  /不重复补账；简短一行（三源验证摘要 + 无新增待办 + 等待项）每次唤醒仍写——memomics-1c1890da #2-#21 连续 20 次
  实证如此（每条 ~4-5 行，20 条后 task_plan 已 ~236 行，逼近 250 压缩阈值）。
- 💡 **已有合并块时的轻量追加：扩块范围而非新建块（唤醒 #88 实测 2026-08-09）**：task_plan 末尾是合并块（如 `## 🏁 唤醒 #80-#87 记录`）且新记录内容与前记录逐字相似（三源验证一致 + 无新待办 + 等待项）时，不必追加新块（块数 +1 只会更快逼近 ≥5 合并阈值）——直接把块标题范围扩为 `#80-#88` + 在块内补一行 `- #88（时间）：三源验证与 #87 一致，终态保持现状`，块数不变、篇幅不涨。⚠️ 前提：块标题是范围格式（`#NN-#MM 记录`）才可扩，单条标题块仍按常规追加；锚点用标题行本身（唯一，无重复歧义），补行锚点用块内最后一行 + 后续 `## 红线与约束` 标题，均 #88 实测一次成功。此法与 #67 的「紧凑记录引用前条」互补：紧凑记录省行数，扩块省块数。
- 💡 **就地更新合并块 bullet（唤醒 #107 实测 2026-08-09，篇幅零增长变体）**：终态 + 合并块内最后一条 bullet 已覆盖相同终态验证时，不必扩块加新 bullet（文件仍 +1 行），直接把该 bullet **就地更新**——range 号 +1（`#105-#106` → `#105-#107`）+ 内容同步最新三源验证差异。本唤醒实测：patch 锚点 = bullet 行本身（唯一），一次成功，task_plan 行数零增长；记录差异点 = 高权限 [Insufficient Permissions] PID 4→3（46332 消失，只留 1120/9108/20832）。优点：篇幅不涨（终态记录链完全不再生长）+ 审计号连续 + 锚点唯一。✅ 前提：三源验证与前条逐项一致 + 无新指令 + 无实质新证据（只是时间/编号/个别数字变化）。⛔ 代价与边界：**就地更新覆盖上一条的独立证据**（如 46332 的细节永久丢失）——终态记录已被反复压缩、逐条审计价值趋近零，可接受；但若本轮验证出现**实质性差异**（新 PID/新产出/新用户指令）→ 必须扩块新增 bullet 保留差异，禁止就地覆盖。
- 💡 **单条记录块 → 范围块变体（唤醒 #109 实测 2026-08-09）**：扩块法（#88）原适用对象是**合并块**（标题已是范围格式 `#NN-#MM 记录`）、就地更新（#107）对象是合并块内 bullet；当末尾是**单条记录块**（标题 `## 🏁 唤醒 #NN 记录`，非范围格式）且新唤醒三源验证与前条逐项一致时，可直接把标题改为 `#NN-#MM 记录` + 正文合并为最新验证内容，**一个 patch 完成**（锚点 = 单条标题行，唯一；new_string = 新标题 + 合并正文）——块数不增（避免逼近 ≥5 合并阈值）、审计号连续、篇幅零增长。本唤醒实测：`#108 记录` → `#108-#109 记录`，正文 = 三源一致摘要 + 等待项，一次成功。与 #107 就地更新的边界：若本轮验证有实质差异（新 PID/新产出/新指令）→ 仍按 #107 规则扩块新增 bullet 保留差异，禁止用合并正文覆盖独立证据。✅ **范围块 + 双 bullet 混合变体（唤醒 #118 实测 2026-08-12）**：单条块 `## 🏁 唤醒 #117 记录` → 标题改 `#117-#118 记录` 时**保留 #117 原 bullet + 追加 #118 新 bullet**（不合并成单段正文）——适用场景：新唤醒验证与上条**有细节差异**（基线组件 14→12、PID 名单变化）但结论同为终态；此时 #109 的「合并正文为最新」会丢 #117 的独立证据、#107 的「扩块加 bullet」又仅为合并块设计 → 混合变体 = 单条块转范围块 + 双 bullet，一个 patch（锚点 = 标题行 + 原 bullet 两行连续）完成，块数不增、两组验证细节都在。与 #109 纯合并变体的选择判据：验证逐字一致 → 合并正文；验证有数字/PID 级差异 → 双 bullet 保留。
- ✅ **完全合并块形态（无 bullet）——取号直接看标题 MM，skip 直接适用（唤醒 #110 实测 2026-08-09）**：memomics-1c1890da 的合并块已彻底压缩为**单段落**（标题 `## 🏁 唤醒 #25-#109 记录`，正文无 `- #NN` bullet、无逐条记录，只有一段三源验证综合描述 + 一条「📦 历次压缩动作」注）——此时：① 最大写盘号 = 标题范围 MM（#109），**无块内 bullet 可比对**，#84/#90/#107 的「标题 MM vs 块内最后 bullet NN」核对直接跳过（该规则前提是块内存在 bullet 串）；② 三源验证与该段落描述逐项一致（同 5 基线 PID + 2 node、同 GPU 桌面-only 名单、双根磁盘 0 命中）+ 无新指令 → skip 规则**直接适用**（本次汇报位 #110 未写盘，下次仍按最大写盘 #109+1 计）；③ 压缩链历史已并入段落内，未来再压缩无需保留旧压缩注。⚠️ 每次唤醒仍须 read_file 确认块内确实无 bullet——若后续有唤醒向块内追加 bullet，形态即变回 bullet 串，标题滞后核对恢复生效。
- 🔴 **body 行『唤醒 #NN 汇报位』污染 grep 取号（唤醒 #116 实测 2026-08-12，memomics-1c1890da）**：合并动作注会把**汇报位编号**写进 task_plan body（如 `📦 合并动作（2026-08-12 唤醒 #116 汇报位）…`，#116 未写盘），`grep -oE "唤醒 #([0-9]+)"` 会捕获该 #116 → grep 结果**虚高 1**（真实最大写盘 = 最后块标题 MM，本 case #113-#115 → 115）。判据：grep 返回号若只出现在 body 的『汇报位』语境（合并注/引用行）而非 `## 🏁 唤醒 #` 块标题 → next = 最后块标题 MM+1（本次 115+1=116），**不信 grep 号+1**。唯一兜底 = 取号后 read_file 核对最后块标题（#84/#90 规则在此场景首次硬性必要）——body 污染是继 #84 标题截断（虚低）、#90 裸 bullet 不可见（漏报）之后第三种取号陷阱形态：**body 引用虚高**（汇报位未写盘却计入最大号）。⚠️ 若后续唤醒在合并注里也写『唤醒 #NN 汇报位』（汇报位编号每次相同，如都写 #116）→ 污染持续存在，取号必须依赖 read_file 核对。
- 🔴 **扩块法必须原子化：块标题 + bullet 同一 patch 内更新（#92 实测 2026-08-09 补强）**：#88 立规后，#89/#90/#91/#92 四次扩块追加均只加 bullet、未同步块标题——memomics-1c1890da 的 `## 🏁 唤醒 #80-#88 记录` 标题已滞后 4 条（块内实际到 #92）。标题滞后后果：① grep 取号只返回标题起点 #80（#90/#84 已记录，本 skill 取号逻辑靠「max(标题 MM, 块内最后 bullet NN)」兜住，但依赖 read_file 才可见）② 「最后记录块标题」锚点失效（后续 patch 只能锚 bullet 行，不能再锚标题行）③ 压缩时 start_marker 判段错（会从旧标题起切，把已在块内的新 bullet 一起切掉）。补强规则：**扩块追加 = 一个 patch 同时改两处**——old_string = 块标题行 + 块内最后一条 bullet 行（两行连续），new_string = 新标题范围行（如 `#80-#92 记录`）+ 原 bullet + 新 bullet。标题与 bullet 原子更新，杜绝「只加 bullet 不更新标题」。若读 plan 时发现标题已滞后（块内最后 bullet NN > 标题 MM）→ 先补账 patch 把标题改为 `#80-#NN 记录` 再追加，锚点直接用标题行（唯一）。
🔴 **标题滞后补账与追加决策解耦（#100 实测 2026-08-09）**：skip 规则成立（三源逐项一致 + 无新指令 + 追加内容逐字相同 → 跳过追加只汇报）时，**标题滞后仍必须补账**——补账是 plan 卫生动作，不依赖本次是否写记录。实测：memomics-1c1890da 块标题 `## 🏁 唤醒 #80-#94 记录` 但块内 bullet 已到 #98-#99（#96/#98 台账已记滞后 2 次仍未修），本唤醒（#100）按 skip 规则跳过追加 → 标题滞后继续残留，下次唤醒 grep 取号/「最后记录块标题」锚点仍被坑。规则：读 plan 发现「块内最后 bullet NN > 标题 MM」→ **无论追加还是 skip，同一轮 patch 标题为 `#80-#NN 记录`**（锚点=标题行本身，唯一；skip 时补账只改标题不加 bullet，不占记录号、不制造冗余记录）。
🔧 **后证（2026-08-09 skip 唤醒 read_file 实测）：标题滞后已修复，勿再补账**——本唤醒读 memomics-1c1890da task_plan，块标题已是 `## 🏁 唤醒 #80-#99 记录` 且块内最后 bullet 为 `#98-#99` → 标题 MM=99 与块内 NN=99 一致，**无滞后**（#100 注里的"残留"表述已过期，未来唤醒读到 `#80-#99` 标题不要再当滞后处理）。⚠️ **该"已修复"声明已被 #101 再次推翻（2026-08-09 实测，见台账缺口 #16）**：本唤醒读 plan 时标题 `#80-#99` 但块内 bullet 已到 #100，且追加 #101 时既未补账旧滞后也未同步标题 → 标题 MM=99 < 块内 NN=101，滞后重新存在。**教训：此类"已修复"状态注记只在写注那一刻为真，每次唤醒都必须 read_file 全文核对「标题 MM vs 块内最后 bullet NN」，不能信 SKILL.md/台账里任何"已修复"声明。**本次唤醒同时验证 skip 规则全流程干净执行：skill_view 先行（Step 0 门禁）→ 显式 E:/ 路径 ls -lt 定位 → read_file 全文 → 三源一致（7 基线组件 webui×2+guardian×2+_kernel_worker.R+node×2 / GPU 3% 桌面-only+4 [Insufficient Permissions] / find 排除后 0 命中）→ 4 合并块 <5 无需压缩 → skip 规则成立（三源一致+无新指令+追加逐字相同）→ 跳过追加只汇报（汇报位 #100 未写盘，下次仍按最大写盘号 #99+1 计）；三查菜单（越线/prep 中间档/保持现状）已附。
- 🗜️ **终态唤醒记录压缩规程（task_plan >250 行时执行；memomics-1c1890da 唤醒 #23 后实测 244 行，已临近阈值——下次唤醒追加记录前 `wc -l <task_plan.md>` 若 >250 必须立即执行本压缩规程，不再标注"预备/尚未触发"）**：
  - 🔗 唤醒链一致性基线更新（#23 实测 2026-08-09）：常规终态唤醒全流程模板执行成功——plain tasklist 仅系统组件（python 35096=webui server 260MB + Rscript 49380=_kernel_worker.R 124MB + 3 小 python）、GPU 3% 空载（compute-apps 全桌面 C+G）、`find -newermt`（手填 plan mtime "2026-08-09 03:58"）仅命中 task_plan 自身；追加 #23 记录锚点 =「#22 尾部重复待办行 + \n\n## 红线与约束 唯一标题」一次成功（再次验证 #17 锚点法）。下次唤醒以完整系统组件基线为准（唤醒 #25 实测 CIM 全量枚举 2026-08-09，memomics-1c1890da）：**webui server×2（PID 3480=.venv python + 35096=Python312 python，均跑 webui\server.py）+ cellbender_guardian×2（PID 28516=.venv + 47856=Python312）+ _kernel_worker.R×1（PID 49380，Rscript 跑 R-4.4.2 持久内核）** = 5 个常驻系统组件，无分析进程。若 plain tasklist 返回空或仅部分命中 → 假阴性（#11/#13/#25 跨唤醒检测器），立即 Get-CimInstance 复核。任何 python/Rscript 进程其命令行含**分析脚本路径**（archr_script.R / l1_phylop / heartbeat / cellbender remove-background）才算活跃分析。
  每条终态唤醒记录 ~4-5 行且内容逐字相似，20+ 条后必然超阈值。压缩时机 = 追加下一条记录前
  `wc -l <task_plan.md>` >250。做法：
  💡 **压缩不必死等 250 行——累积 5+ 条逐字相似记录即提前合并（唤醒 #33 实测 2026-08-09）**：上次压缩（#26 压 #2-#24）后 #27-#32 又追加 6 条与 #25 逐字相同的终态记录，plan 仅 175 行（<250）但冗余已明显 → 提前将 #25-#32 八条合并为一条（保留最新 #32 细节），plan 降至 ~145 行。判据 = 非行数而是**重复率**：新记录与既有记录\"逐字一致仅时间戳/数字不同\"且成块出现 → 直接压缩，不必等超阈值再处理（避免每次唤醒都背着 20+ 条冗余）。⏰ **追加后立判（唤醒 #37 实测 2026-08-09 补录）**：#33 压缩 #25-#32 后，#33-#37 又累积 5 条逐字相同终态记录，#37 追加时漏合并 = 该规则执行缺口。判据收紧为：**每次追加记录后立即数逐字记录块，≥5 条 → 同轮合并旧 4 条（保留最新一条）**，不等 250 行、不等下一次唤醒——memomics-1c1890da 的 #33-#38 已合并（唤醒 #39 追加前 plan 已见合并记录，5 条→1 条）；**#39/#40/#41 已累积 3 条逐字相似终态记录（各 ~2-3 行，未达 ≥5 阈值不压缩）**——按\"≥5 条 → 同轮合并旧 4 条保留最新\"规则，**#42 追加后达 4 条、#43 追加后达 5 条 → #43 追加时必须合并 #39-#42**；task_plan 当前 ~162 行（<250，行数无压力，纯重复率判据）。下次唤醒追加前 `wc -l` 复核即可。
  🔴 **#43/#44 连续漏合并（2026-08-09 实测，执行缺口第 2/3 次）**：#39-#42 四条逐字相似记录已存在，#43 追加时未合并 → #44 追加时又未合并（skill 明文要求"下条追加前必须处理"仍落空）→ plan 现有 #39-#44 **六条**逐字相似终态记录（各 ~2-3 行）。教训：**"≥5 条 → 同轮合并"规则写在 skill 里不代表执行时会触发**——追加动作与压缩动作是两个独立步骤，Agent 写完记录就收尾了，没做第二步。强制化为**原子两步（先数后加）**：**追加前先 `grep -cE '^## 🏁 唤醒 #.*记录' task_plan.md` 数现有记录条数；若逐字相似块 ≥5 → 先合并旧 4 条再追加新记录，绝不在 ≥5 时先追加后合并**。两步合并成一步执行，消除"写了就收尾"的缺口（#39-#44 六条留给下次唤醒追加前合并）。
  - 📌 **#48 追加后状态（2026-08-09 实测）**：#45-#48 四条逐字相似终态记录已累积（#45 含压缩动作行，其余各 ~2-3 行），未达 ≥5 阈值不压缩；**下次唤醒 #49 追加前若达 5 条 → 先合并 #45-#48（保留最新 #48 细节）再追加**；plan 行数 ~173（<250，行数无压力，纯重复率判据）。
🔴 **#49/#50 连续两次漏合并，终态块已达 6 条（2026-08-09 实测，执行缺口第 4/5 次）**：#48 状态注已明示\"#49 追加前若达 5 条 → 先合并 #45-#48\"，但 #49 追加时**未合并**（#45-#49 = 5 条已越阈值）；本次唤醒 #50 又直接追加**未合并**（#45-#50 = 6 条逐字相似终态记录，task_plan 行数 ~177）。教训复现：\"先数后加\"规则反复落空，**追加动作总是优先于压缩动作**。强制再升级：追加前**必须**执行 `grep -cE '^## 🏁 唤醒 #.*记录' task_plan.md` 数记录块数（注意只用 `^##` 锚定标题行，勿用无锚定 grep 计数——body 里的\"与唤醒 #N 一致\"引用会污染计数）；若 ≥5 → **先合并旧 N-1 条（保留最新一条）再追加**，绝不在 ≥5 时先追加后合并。**下次唤醒 #51 追加前：立即合并 #45-#49 五条为一条（保留最新 #49 细节），再追加 #51**。⚠️ 合并动作本身算一次执行，写完合并记录后若又追加新记录导致再次 ≥5，仍需再合并——压缩不是一次性动作，是每次追加的常态化前置。
  - 📌 **#56 追加后状态（2026-08-09 实测）**：#55 已执行 #54 注指示的压缩（task_plan 记录 "📦 压缩动作：#51-#54 四条逐字相似终态记录合并为一条（保留最新 #54 细节）"，压缩链在 #55 处正确闭合）→ 当前记录块 = **3 块**（#51-#54 合并块 + #55 + #56 单条），未达 ≥5 阈值不压缩；**下次唤醒追加 #57 后达 4 块、追加 #58 后达 5 块 → #58 追加前必须先合并旧 4 条（保留最新 #57 细节）再追加 #58**。plan 行数 ~160（<250，行数无压力，纯重复率判据）。锚点法第 9 次实证（#17→#26→#28→#31→#33→#37→#41→#54→#56）：#56 锚点 = #55 尾部待办行 + `## 红线与约束` 唯一标题一次成功；#56 追加前用 read_file 全文读 task_plan（155 行）即完成"先数后加"的块数清点（3 块 < 5 → 合法单追加，未触发压缩 = 正确行为，勿因"没压缩"自责）；三源验证再次命中 5 基线组件（webui×2 PID 3480/35096 + guardian×2 PID 28516/47856 + _kernel_worker.R PID 49380）+ 2 node（48280/19156），GPU compute-apps 全桌面 C+G（含 Qianwen×2/Doubao/Steam/Olivia×2 新面孔），磁盘 find -newermt "2026-08-09 08:00" 无命中（最新=01:49 heartbeat 终态）。
  - 📌 **#59 追加后状态（2026-08-09 实测）**：#57/#58 已由后续会话追加（#58 记录内含"📦 压缩动作：#25-#56 四条记录块合并为一条（保留最新 #56 细节），#57 保留"，压缩链正确闭合）→ 本唤醒 #59 追加前 read_file 清点 = **4 块**（#25-#56 合并块 + #57 + #58 单条 + 本次 #59），<5 合法单追加，未触发压缩；plan 行数 ~159（<250，行数无压力，纯重复率判据）。**下次唤醒 #60 追加后达 5 块 → #60 追加前必须先合并旧 4 条（保留最新 #59 细节）再追加 #60**。锚点法第 10 次实证（#17→#26→#28→#31→#33→#37→#41→#54→#56→#59）：#59 锚点改用**最后记录的 📦 压缩动作行**（该行唯一，仅最新记录携带）+ `## 红线与约束` 唯一标题，一次成功——锚点不必拘泥"尾部重复待办行"，最后记录内任何唯一行 + 红线标题均可。
  - 📌 **#60/#61 状态 + 追加跳过决策（2026-08-09 实测，LoopX 零基头 #0）**：#60 已由后续会话追加（task_plan 现含 #60 单条终态记录）→ 当前记录块 = **2 块**（#25-#59 合并块 + #60），远低于 ≥5 阈值，无需压缩。⚠️ 本唤醒（LoopX #0）三源验证与 #60 逐项一致（GPU 4% 空载全桌面 C+G、5 基线组件 PID 3480/35096/28516/47856/49380 + 2 node、磁盘 find 无命中）→ **未追加 #61 短记录**——这是对「终态纯汇报也追加一行」的合规豁免。**跳过追加的决策规则（与 skill 内两条规则的矛盾解）**：仅当 ① 最新终态记录为当日/近时且三源验证逐项一致（同基线 PID、同 GPU 桌面-only、同磁盘无新产出）② 无新用户指令 ③ 追加内容将与前记录逐字相同（只会制造待压缩冗余）——三者全满足 → 可跳过追加，只汇报；任一不满足 → 仍追加短记录。✅ **skip 规则 #81 二次实证（2026-08-09 LoopX #2，首次真正执行）**：三源验证与 #80 逐项一致（同 5 基线 PID、同 GPU 桌面-only 名单、同磁盘 find 0 命中）+ 无新用户指令 + 追加内容与前记录逐字相同 → 跳过追加、只汇报，并在汇报里明确写出"本次跳过追加重复终态记录（#60/#61 合规豁免规则）"。✅ **skip 规则三次实证（2026-08-09 LoopX 零基头 #0，对应 #85 汇报位未写盘）**：同模式再次验证通过——三源与 #80-#84 合并块逐项一致（同 5 基线 PID + 2 node、同 GPU 桌面名单含全行 [N/A] 显存、find 排除后 0 命中）、task_plan 无新指令、追加内容与前记录逐字相同 → 跳过追加只汇报；本 skill 无需每次为 skip 追加新注记，规则已稳定（#81/#85/#87 三次真实执行）。⚠️ **汇报编号 ≠ 写盘记录号**：跳过追加时回复标题里的"唤醒 #N"只是汇报编号，未写盘不占用记录号——下次唤醒仍按最大写盘号 +1 计算（#81 汇报未写 → 下次仍 #81），编号只在写盘后才具权威性，无需担心撞号；也不要把"汇报了 #81"当成本次已记账。
🔴 **report-only 会话同样必须 Step 0 skill_view + 合规进程源（缺口 #20 实测 2026-08-09）**：skip 规则下不写盘 ≠ 可以放松——本会话（#110 汇报位）跳过追加但**对话汇报本身**写了"tasklist 0 命中"（实际是禁变体 `tasklist //FI ... 2>/dev/null` 空输出，未 plain 复核、未列 5 基线组件）→ 汇报文本与写盘记录同等权威，同样污染后续会话。判据：**无论追加还是 skip，进程源必须 plain `tasklist 2>/dev/null | grep -iE 'python\.exe|Rscript\.exe|node\.exe'`（或 verify_wakeup.sh），汇报与记录都必须带基线组件清单**；不写盘只豁免"记录合并/压缩"义务，不豁免三源纪律。⛔ 滚动块数不再在 skill 维护（会过期），每次唤醒以 read_file 清点 task_plan 实际记录标题行为准（当前 2 块；#61 若追加 → 3 块仍 <5）。**#61 已实际追加（LoopX #3 于 2026-08-09 追加，锚点=#60 尾部待办行+`## 红线与约束` 唯一标题一次成功）→ 当前 3 块 <5 无需压缩；下次唤醒编号 = #62，以 read_file 清点为准，勿信本注记的旧块数。**
  - 📌 **#62-#67 连续追加后状态（2026-08-09 实测）**：#62-#66 由后续唤醒逐次追加（未合并，#63/#64/#65 已属执行缺口），#67 又追加 → task_plan 现有 **9 块**终态记录（#25-#59 合并块 + #60-#67 八条单条），**远超 ≥5 阈值**。⛔ **#67 追加前仍未先合并 = 执行缺口延续**——"先数后加"规则第 N 次落空（追加动作总优先于压缩动作，结构性问题）。**下次唤醒 #68 追加前必须：① read_file 清点实际块数（勿信本注记）② 合并 #60-#67 八条为一条（保留最新 #67 细节）③ 再追加 #68**。
  - 🔴 **#68 实测（2026-08-09，单斜杠 `/FO CSV` 本机可用实证 + 压缩缺口延续第 9 次）**：进程源首查用 `tasklist //FO CSV`（双斜杠、未加 2>/dev/null）→ **乱码错误可见 `����: ��Ч����/ѡ�� - '//FO'`**——与 #9 `//FI` 同因：本机 bash 不把双斜杠转回单斜杠，tasklist 收到字面 `//FO` 直接拒绝。**改用单斜杠 `tasklist /FO CSV 2>/dev/null | grep -iE "python|Rscript|R\\.exe|cellbender|torch"` → 一次成功列出全部 5 基线组件（python 3480/35096/28516/47856 + Rscript 49380）**。根因闭环：既往 #25/#46/#62/#65 的"`//FO CSV` 假阴性"其实是**双斜杠报错 + `2>/dev/null` 吞 stderr → 空输出**；stderr 保留时错误可见，单斜杠 `/FO` 本机可用（CSV 带引号进程名，可 awk 解析）。技能对 `//FO CSV` 的禁变体立规修正为：**双斜杠 `//FO` 必错（禁）+ `2>/dev/null` 吞错必假阴性（禁）**；`/FO CSV`（单斜杠）可作 parseable 替代，但 plain `tasklist | grep -iE '^python|^Rscript|^node'` 行锚定仍是零歧义首选。#68 追加前 read_file 清点 = **9 块** > ≥5 阈值，**仍未先合并就追加（缺口第 9 次，结构性病根不变：追加动作先于压缩动作）**；下次唤醒 #69 追加前必须合并 #60-#68 九条为一条（保留最新 #68 细节）再追加。
  - 💡 **紧凑记录 = 与上一条验证逐项一致时引用前条，不重复全文（唤醒 #67 实测 2026-08-09）**：三源验证与上一条逐项一致（同 GPU 空载 / 同 5 基线组件 PID / 同磁盘无新产出）时，新记录不必重复整段验证文本——开头写"三源验证与 #66 一致"即可大幅压缩篇幅。实测 #67 记录仅 2 行（引用 #66 + 等待项），比 #60-#66 各 4-5 行逐字重复明显更瘦。**但这只是缓解不是替代**：即使写紧凑记录，块数仍从 8 涨到 9（引用前条 ≠ 不记账），≥5 阈值合并仍必须执行——紧凑记录的价值是降低"逐字相似块"的冗余度，让合并时保留的内容更短。锚点法第 12 次实证（#17→#26→#28→#31→#33→#37→#41→#54→#56→#59→#61→#67）：`无新增待办…\n\n## 红线与约束`（尾部重复待办行 + 唯一标题）一次成功。
  🔴 **阈值边界陷阱（唤醒 #25 实测 2026-08-09）**：追加前 `wc -l` = 248（<250 不触发压缩），但追加 5 行记录后 = 253 > 250——**"追加前 >250 才压缩"的判据会在 245-249 行区间漏掉"本次追加本身越线"**。正确判据：`wc -l` + 预计新增行数 ≥ 250 → 先压缩再追加（或把新记录压缩到 ≤ 250-当前行数）。248 行时已 98% 阈值，下条记录必越线，应主动在本次追加前压缩 #2-#24 重复终态记录。
  ① **保留**：文件头（Goal/Environment/Current Phase）+ 待办清单各 Phase 的 Status + 第一条完整终态总结
     （如"🏁 测试版执行总结"）+ **最新一条**唤醒记录 + 红线与约束 + Errors/Decisions；
  ② **删除**：中间重复的终态唤醒记录（#2 至 #N-1，与最新一条逐字相同的内容）；
  ③ **锚点**：整段删除用 patch 以相邻唯一标题为界（如从某条记录标题行到"## 红线与约束"之前），
     ⛔ 不要拿共享重复行（如"- 无新增待办，等待用户指示正式版…"）当 old_string（不唯一报错）；
  ④ **记账**：压缩动作写入新唤醒记录（"压缩 #2-#N-1 重复终态记录"），保持 plan 可追溯。
  - ✅ **压缩实测执行路径（唤醒 #26 实测 2026-08-09，memomics-1c1890da）**：`execute_code` Python 字符串切片法一次删 100+ 行重复记录，
    比 patch 构造 100+ 行 old_string 更稳——① `s = content.index(start_marker)`（要删的第一条记录标题，如 `## 🏁 唤醒 #2 记录（2026-08-09 01:50 终态确认）`）
    ② `e = content.index(end_marker)`（要保留的第一条记录标题，如 `## 🏁 唤醒 #25 记录（...）`）③ `new_content = content[:s] + content[e:]` 写回
    ④ 验证：`wc -l` 252→147 + 逐个断言关键标题仍在（Goal/Environment/Current Phase/第一条完整总结/最新记录/红线/Errors/Decisions）→ 再 patch 追加新记录。
    ⚠️ **压缩后验证勿用 MSYS grep 数中文标题**：`grep -c '^## 🏁 唤醒 #'` 对中文内容返回 0 = 编码假阴性（本唤醒实测），
    用 read_file 直接读记录行或 search_files 确认真实存在。
  - 🔴 **grep 计数记录块在本机同样不可用（唤醒 #52 实测 2026-08-09）**：追加前数块用的 `grep -cE '^## 🏁 唤醒 #.*记录' task_plan.md` 对中文标题返回 **0**（与 #26 压缩后验证同因的编码假阴性——`grep -c` 对中文内容数不出行）→ 数记录块**必须用 read_file 读记录段标题行数**（本唤醒实测 5 块：#25-#32/#33-#38/#39-#44/#45-#50/#51 → 触发 ≥5 先合并旧 4 条再追加 #52）；⛔ grep 返回 0 不可作为"记录数少"证据，更不能据此跳过压缩。
  - 🔴 **search_files 内容搜索同样假阴性（唤醒 #61 实测 2026-08-09）**：`search_files(target=content, pattern="唤醒 #\d+")` 搜 task_plan.md 返回 **0 条**，但文件明明含"唤醒 #60 记录"——与 grep -c 同因的编码假阴性。⛔ **数/验唤醒记录唯一可靠手段 = read_file 全文读记录段标题行**；✅ **execute_code + Python re.findall 可编程计数同样可靠（#80 实测 2026-08-09）**：`re.findall(r'^## 🏁 唤醒 #[\d#\-]+ 记录.*$', content, re.M)` 数块数与 read_file 人工数一致（9 块），Python 读 UTF-8 无 MSYS grep 编码假阴性；且与 #52 合并+追加原子法同脚本衔接（数块→断言→切片合并→写回→断言），比"read_file 数块 + 单独脚本合并"两步更省。search_files 内容搜索中文标题（尤其含 `#` 的正则）返回 0 不可作"记录不存在"证据，skill 早前"用 search_files 确认真实存在"的建议作废。
  - ✅ **合并+追加单次原子化（唤醒 #52 实测 2026-08-09，memomics-1c1890da）**：execute_code 一次完成压缩+追加两件事——① 字符串切片合并旧记录（start=最旧合并块标题，end=最新单条记录标题，中间插入合并块）② `content.replace("## 红线与约束", new_record + "\n\n## 红线与约束", 1)` 同脚本追加新记录 ③ 写回 + 断言验证（本唤醒 166→157 行，关键标题全在）→ 从机制上消除"写完记录就收尾漏压缩"缺口（#37/#43/#44/#49/#50 连续五次执行缺口的结构性解法）。
  - ✅ **patch 整块替换也是可靠压缩路径（唤醒 #33 实测 2026-08-09，与 #26 的 execute_code 切片法并列）**：8 条记录（#25-#32）
    合并为 1 条时用 patch 一次替换整块（old_string = 全部 8 条完整文本，new_string = 合并条 + 新 #33 条）→ 一次成功。
    适用边界：**中等块（<80 行）且已 read_file 全文**（本 session 先 offset 读 1-120 + 121-175 两段拿到完整内容）→ patch
    构造 old_string 可行；超大块（100+ 行）仍走 #26 的 execute_code 字符串切片法（index(s)+index(e)+拼接）。两种方法按块大小选。
  - ✅ **regex 正则合并 = 第三种压缩路径（唤醒 #105 实测 2026-08-09，合并块内裸 bullet 串首选）**：当要合并的记录是**已合并块内的裸 bullet 串**（如 `- #88（11:5x）：...` 到 `- #104（唤醒 #0 LoopX）：...终态 ✅，保持现状`，各条无独立 `## 🏁` 标题、无唯一切片起止锚）时，patch 需构造上百行 old_string、#26 切片法又缺标题锚——用 `execute_code` + `re.search(r"- #88（11:5x）：.*?- #104（唤醒 #0 LoopX）：.*?终态 ✅，保持现状", content, re.DOTALL)`（**非贪婪 `.*?` + DOTALL**，起锚=首条 bullet 标记，止锚=末条 bullet 的终止短语）→ 命中整串 → `content[:m.start()] + 合并条 + content[m.end():]` 写回，同一脚本内验证行数。实测 178→164 行、17 条 bullet（#88-#104）合并为一条 + 追加 #105 单脚本完成。适用边界：目标 = 无独立标题的裸 bullet 连续串（#26 切片法适用对象是有独立 `## 🏁` 标题的记录段）；⚠️ 止锚短语（如 `终态 ✅，保持现状`）在串内多条 bullet 重复 → 非贪婪让第二个 `.*?` 停在第一条末 bullet 处即可，或把末条 bullet 的独特编号/时间戳也包进模式；⚠️ 合并条不保留逐条原文——写压缩摘要（范围 + 三源验证一致 + 最新细节），否则合并无意义。
  - ✅ **锚点法第 5 次实证（#17→#26→#28→#31→#33）**：追加新唤醒记录用「上一条尾部重复待办行 + `\n\n## 红线与约束` 唯一标题」当 old_string 一次成功——#31 锚点 = #30 尾部待办行 + 红线标题（唤醒 #31 实测 2026-08-09），同样一次成功；#31 还实测消息头 `[系统唤醒 #0]`（LoopX 零基计数器）→ 实际记录号 = 已有最大 #30 + 1 = #31，零基头照样按"最大号+1"处理，不抄头、不从 0 重计；#28 的 plain tasklist grep 也直接列出全部 5 系统组件（webui×2 + guardian×2 + _kernel_worker）无假阴性 → 该锚点法持续可靠（5/5 实证），plain grep 全命中即可免 CIM 复核。#33（2026-08-09 实测）再次确认零基头 `[系统唤醒 #0]` → 记录号 = 最大 #32 + 1 = #33，且用同法锚点追加成功（第 5 次实证）；#37（2026-08-09 实测）同锚点法一次成功（第 6 次实证）；#41（2026-08-09 实测）同锚点法**第 7 次实证**（零基头 `[系统唤醒 #0]` → 记录号 = 最大 #40 + 1 = #41；锚点 = #40 尾部待办行 + `## 红线与约束` 唯一标题一次成功；find 基线 = task_plan 自身 mtime 05:21，排除自身后无命中 → 终态不追加重复总结，仅简短记录），plain tasklist grep 命中全部 5 系统组件（PID 3480/35096/28516/47856/49380 = webui×2+guardian×2+_kernel_worker）→ 与 #25/#34 完整基线一致，比 #35/#36 记录的"无匹配"更可靠——**终态记录优先写完整组件清单；若 grep 对常驻组件返回空，先质疑过滤器再写"无匹配"**（跨唤醒一致性检测器的记录侧自检）。
  💡 判断"记录是否可删"的判据：内容与最新记录逐字一致（三源验证摘要 + 无新增待办 + 等待项），
     唯一差异是时间戳/数字 → 可安全删除，最新一条已代表终态全貌。
- 读 task_plan 全文，注意 `Current Phase` / `## 未完成待办` / 用户红线（如"等待用户确认"）。

## Step 2: 终态验证 — 三个高频陷阱

**陷阱 A：陈旧 Status 标记**
task_plan 头部写"✅ 任务已完成"、所有 checkbox 已勾选，但某个 Phase 的 `**Status:**` 行残留 `in_progress`。
→ 先用磁盘产出验证完成声明（硬证据），确认后**顺手 patch 掉陈旧标记**，保持 plan 自洽。
→ ⛔ 不因标记不一致就重跑 Phase。

**陷阱 B：空模块目录 ≠ 产出缺失**
会话目录下某模块子目录（如 `04_xxx/`）为空，但产出实际写在**另一个** `results/{session_dir}/`（跨会话接力常见）。
→ 报缺失前先查 memory、log/system_log.jsonl、相邻 session 目录确认产出真实位置。
→ ⛔ 看到空目录就报警 = 误报。

**陷阱 C：完成声明 ≠ 完成事实**
task_plan 说"已完成"但可能是上次 Agent 的乐观记录。
→ 每个模块至少抽查 1-2 个关键产出文件真实存在且非空（rds/csv/png 的大小）。
→ HTML 报告、最终图等交付物单独确认存在。

**陷阱 D：批量产物（40/40 式）完整性两条命令验证，别逐样本读日志**
批处理（多样本 QC / 去污染 / 下载）宣称完成时，用「汇总行 + 目录数」双重确认：
→ `ls -d <输出目录>/<样本前缀>* | wc -l` 数目录/文件数，与计划数比对（如 40）。
→ 若批处理写过汇总 CSV（如 `QC_summary_all40.csv`），`tail -2` 看 TOTAL 行：
   `TOTAL,265909,,40/40` 自带完成标记；TOTAL 行不存在或不是 N/N → 批处理未完成。
→ 2026-08-08 唤醒 #4 实测（GSE278576 40 样本 QC）：两条命令即可确认完成态，无需读 40 个样本日志。
→ ⛔ **数目录 ≠ 数条目（唤醒 #17 实测）**：`ls <dir> | wc -l` 会把 summary CSV 等非目录条目计入
   （40 样本目录 + QC_summary_all40.csv + 1 杂项 = 42 条目）。判"N/N"用 `ls -d */ | wc -l` 数**目录**，
   非目录条目逐个点名，禁止把"42 条目"直接报成"40 样本"。

**陷阱 E：未勾 `[ ]` 待办 ≠ 未完成 — 磁盘产出优先于 plan 标记（唤醒 #0 实测 2026-08-09）**
task_plan 里某 Phase 标 `in_progress`、某 todo 还勾 `[ ]`，但**实际工作已由上一会话完成**——plan 只是忘了同步。
→ 先按 mtime 找产出：`ls -lt <工作目录>/` + `find <工作目录> -name '<产出模式>'`，把"plan 声称未完成"与"磁盘时间戳"对照。
→ 实测案例：P3 剩"猴侧 JASPAR motif"未勾，但 `p3_l1_motif.R` + 4 个输出 CSV（01:30-01:32 mtime）都在 → 实际已完成；
   P6 专利文档标 pending，但 patent/ 下三份文档已生成（01:31-01:32）→ 实际已完成。
→ 判定规则：**文件 mtime 晚于 plan 最后更新或接近 → 以磁盘为准**，patch 同步 plan（勾掉、标 completed），不要重跑。

**陷阱 E2：patch task_plan 时可能被并行 sibling subagent 修改（2026-08-09 实测）**
唤醒同步 plan 时，另一个 subagent 可能同时在写同一个 task_plan.md（本会话 patch P6 段失败，系统提示文件 01:32:36 被 sibling `4e5e409c` 修改过）。
→ patch 失败信息若含 "modified by sibling subagent" → **先 re-read 再决定**：目标段可能已被对方同步（本次 P6 已被对方标 completed，直接跳过即可），不要硬 patch 覆盖对方更新。
→ 多个 patch 分批发时，把容易冲突的段落（如 Status 行）放在最后，先做低冲突追加。

**陷阱 F：脚本无 log 时，从输出 CSV 重算关键统计，不照抄 plan 叙述（2026-08-09 实测）**
脚本跑完但 stdout 未落盘（无 .log 文件）→ task_plan 里只有叙述性结果（如"Jaccard 0.020"），无法核对。
→ 重算路径：读输出 CSV（如 motif rank 文件）→ 按脚本逻辑重算关键统计（top30 重叠、Jaccard、共享清单）→ 写回 plan 时带重算结果。
→ 这同时验证了产出物可读 + 结果可复现，比信任 plan 叙述更可靠（plan 数字可能来自中间版本）。
→ ⚠️ 重算时注意脚本用 `utf-8-sig` 读 CSV（BOM），Windows 路径用 `E:/` 而非 `/e/`（execute_code 沙箱不识别 MSYS 路径）。

## Step 3: 三源交叉验证

💡 **三源验证脚本 scripts/verify_wakeup.sh（#82 后新增 2026-08-09）**：`bash scripts/verify_wakeup.sh <会话目录> [基线时间]` 一条命令跑完三源（进程 plain tasklist + GPU compute-apps + find 磁盘），不需要手打命令、不需要翻历史。🔴 **verify_wakeup.sh 实际路径在 skill 目录，不在仓库根（2026-08-12 唤醒 #110 实测）**：从 `MEMOMICS_HOME` 跑 `bash scripts/verify_wakeup.sh ...` 报 "verify_wakeup.sh 不可用"（脚本真实位置 = `MEMOMICS_HOME/hermes_home/skills/bioinformatics/wakeup-progress-check/scripts/verify_wakeup.sh`，skill_view 的 linked_files 可见）。**失败不是脚本坏，是路径错** → 回退路径 = Step 3 快查三条（本唤醒实测等效可靠，见下），或显式 `bash "MEMOMICS_HOME/hermes_home/skills/bioinformatics/wakeup-progress-check/scripts/verify_wakeup.sh" <会话目录> <基线>`。🔴 **#82 复发实证（2026-08-09，执行缺口第 13 次）**：唤醒 #82 未 skill_view 本 skill 直接凭 memory 跑三源 → 进程源又用被禁 `tasklist //FI "IMAGENAME eq python.exe|Rscript.exe|cellbender.exe|node.exe" 2>/dev/null | grep` 四连 → 全空输出 → **未补 plain 复核** → 记录 #82 写 "0 命中 python/Rscript/CellBender/node" 且未列 5 基线组件（webui×2 PID 3480/35096 + guardian×2 PID 28516/47856 + _kernel_worker.R PID 49380，同 #62/#65/#76 数据污染模式——终态结论靠 GPU 空载 + 磁盘无新产出两源兜住，碰巧正确，但记录 #82 进程源不可信，不得用作跨唤醒一致性佐证）。⛔ 纪律重申：**唤醒第一步 = skill_view 本 skill（#76 立规），三源跑 verify_wakeup.sh 或下方 Step 3 快查三条，禁用任何 //FI //FO CSV / 2>/dev/null 变体；空输出一律 plain `tasklist 2>/dev/null | grep -iE 'python\.exe|Rscript\.exe|node\.exe'` 复核，记录必须带基线组件清单。**
🔴 **Step 3 快查 — 本机可靠命令（#46 实证 2026-08-09，先跑这三条，别翻历史）**：
```bash
# ① 进程源（唯一可靠命令：plain tasklist + 锚定扩展名；期望命中 5 系统组件基线 webui×2+guardian×2+_kernel_worker.R + 2 node = 7 行）
tasklist 2>/dev/null | grep -iE 'python\.exe|Rscript\.exe|node\.exe' || echo NO_ANALYSIS_PROC
# 💡 快照法变体（唤醒 #113/#115 实测 2026-08-12）：先 `tasklist > /tmp/tl.txt 2>/dev/null` 一次快照进程表，再 `grep -iE 'python|Rscript|cellbender|torch|pytest' /tmp/tl.txt` 多次换模式重查——无需重复跑 tasklist；grep 无命中 exit 1 只记在文件 grep 上，不反复触发 tasklist 调用失败计数；快照行 = 同一时刻一致性视图（多模式对比不会因两次 tasklist 之间进程变化误判）。#115 实测：命中 8 行（python×4 = webui×2+guardian×2；Rscript×4 = _kernel_worker.R×4 跨会话累积，无 node/pytest）→ 属 12 基线（kernel.R×4+kernel.py×2+node×2+webui×2+guardian×2）合法子集，判"仅系统组件"成立
# ② GPU 源（--query-compute-apps 轻量、无二进制 grep 坑；C+G=桌面应用，Compute=分析进程）
nvidia-smi --query-compute-apps=pid,process_name,used_memory --format=csv
# ③ 磁盘源（基线 = task_plan.md 自身 mtime；排除 .loopx/jsonl/log/task_plan；find 命中自身=预期不算未记账，#48 实测把 task_plan 加进排除后空输出更干净）
find <会话目录> -newermt "<task_plan mtime>" -type f | grep -vE '\.loopx|token_usage|system_log|\.log|task_plan' | head -20; echo "---FIND-DONE---"
# 💡 磁盘源哨兵（唤醒 #121 实测 2026-08-12）：`| head -20; echo "---FIND-DONE---"` 使 0 命中时 exit 仍 0（head 掩掉 grep exit 1，避免 harness 记失败计数）+ 显式哨兵区分"空输出=0 命中"与"命令失败无输出"——与 tasklist `|| echo NO_ANALYSIS_PROC` 同纪律，find 空输出不再歧义；多根 find（patent_test/ + E:/专利 双根模式）与外部根 find 同样加哨兵
# 💡 项目产出落在 results/ 之外时（本专利项目 E:/专利、骨骼肌项目 E:/骨骼肌锻炼、CellBender PROJECT_DATA_DIR），磁盘源必须对每个外部根再跑一条 find——会话目录 0 命中但外部根有新文件 = 未记账产出；💡 **多根可合并在同一条 find（2026-08-12 唤醒 #120 实测）**：`find <root1> <root2> -newermt "<基线>" -type f` 一条命令同时查 results 内部目录 + 外部根（实测 `find MEMOMICS_HOME/results/memomics-1c1890da/patent_test E:/专利 -newermt "2026-08-12 01:40" -type f`）→ 0 命中即双根均无新产出，比逐根跑更省且等效可靠——双根模式（"patent_test/ 与 E:/专利 均无新产出"）不必再拆两条 find，判终态可单命令完成（2026-08-09 LoopX 唤醒实测：results 与 E:/专利 两条 find 并跑、均 0 命中才判终态；记录 #140 的 "patent_test/ 与 E:/专利 均无新产出" 即此双根模式，Step 3 快查三条原先未写清）
# ⚠️ 手填 mtime 一律 ISO 格式 "2026-08-09 10:47"；⛔ 勿用 $(ls -l task_plan.md | awk '{print $6,$7,$8}') 自动取——本机中文区域 ls 输出 "8月 9 10:47"，find -newermt 无法解析（唤醒 #84 实测报 `cannot figure out how to interpret`），直接手填 ISO 即过
# ✅ `ls -l --time-style='+%Y-%m-%d %H:%M'` 直接输出 ISO（唤醒 #110 实测 2026-08-09）：一条命令拿到干净 ISO mtime（如 "2026-08-09 01:49"），彻底绕开中文区域 "8月 9 10:47" 陷阱——核对产出文件 mtime vs task_plan mtime 时优先用此变体，比手填 ISO 更稳（无手误）、比 awk 自动取更可靠（无区域依赖）
```
💡 **基线组件数合法值 = 5 / 6 / 7（2026-08-09 新会话唤醒 #2 + 唤醒 #110 实测）**：Rscript `_kernel_worker.R`（PID 49380）是 execute_r 持久内核，**惰性启动**——纯 LoopX 唤醒会话若全程未调用 execute_r，该进程不存在；node×2（PID 48280/19156）同样是**间歇组件**（task_plan 记录"部分唤醒 0 命中 node"，#110 实测 kernel 在但 node 0 命中 → **5 项** = webui×2 + guardian×2 + _kernel_worker.R）。三种合法形态：**7 项**（webui×2 + guardian×2 + kernel + node×2）、**6 项**（kernel 缺、node 在）、**5 项**（kernel 在、node 缺）。汇报时**明确写出实际命中组合**（如"webui×2 + guardian×2 + _kernel_worker.R，node 本次 0 命中（部分唤醒 0 命中 node，一致）"）：别把 5/6 当过滤器假阴性去 CIM 补查（会浪费时间），也别把 7 当唯一合法基线；跨唤醒一致性对比时 5/6/7 都算基线命中，只有 0-4 项才触发复核。🔴 **持久内核跨会话累积 → 行数上限被突破（2026-08-12 唤醒 #110 实测 12 行）**：execute_r/execute_code 的持久内核（`_kernel_worker.R`×4 + `_kernel_worker.py`×2 + node×2 comfyui-mcp + webui×2 + guardian×2 = **12 行**）会**跨会话累积**（每次 execute_r/execute_code 会话可能各留一个 worker），进程源行数 >7 是合法常态，**不要**因 >7 行就判"有活跃分析"或触发复核。判据仍是 CommandLine：全部 `_kernel_worker.R/.py` + webui/server.py + cellbender_guardian.py + comfyui-mcp = 系统组件；出现分析脚本路径才算活跃。CIM 一次性列 CommandLine（check_procs.ps1 模式，见下）比逐行猜快。🔴 **pytest 门禁进程 = 合法组件（唤醒 #111 实测 2026-08-12）**：AGENTS.md 要求改代码后跑 `pytest -m "not external..."`（webui/tests offline 门禁）→ 会话期间可能看到 **2 个 python.exe**（`.venv` python + Python312 python，命令行含 `-m pytest`）→ **不是分析进程**，不要误报活跃；行数合法值扩展为 **14 行**（12 基线 + pytest×2）。确认方法（#111 实测一次成功，无需写 .ps1）：`powershell -NoProfile -Command 'Get-CimInstance Win32_Process | Where-Object {$_.ProcessId -in <pid1>,<pid2>} | Select-Object ProcessId,Name,CommandLine | Format-List'` —— 单引号包裹 + `-in` 列表过滤无内部引号嵌套，bash 下可内联（区别于 #29 的 `-Filter "Name like '%x%'"` 引号嵌套陷阱）。
⛔ **进程源只许用上面这条 plain 命令。** `//FO CSV` / `//FI` / 包 cmd / 带 `2>/dev/null` 的过滤变体一律不许试——
#11/#25/#35/#43/#44/**#46** 六次实证假阴性。#46 再犯实录（2026-08-09）：`tasklist //FO CSV 2>/dev/null | grep -iE "python|Rscript|CellBender|torch"` 空输出，
**未按"空输出→质疑过滤器→plain 复核"处置，直接写"无分析脚本进程"进唤醒记录**（且没列 5 基线组件，违背记录侧自检）——
进程源记录降级 = 三源退化为两源，靠 GPU+磁盘兜底才没误判终态。**空输出先质疑过滤器 → plain tasklist 复核 → 复核确认后才能写"无匹配"；记录必须写全基线组件清单。**
🔴 **#76 再犯（2026-08-09 实测，根因 = 未加载 skill 直接凭 memory 跑唤醒）**：唤醒 #76 未 skill_view(wakeup-progress-check)，凭 memory 执行三源验证 → 进程源又用被禁 `tasklist //FI "IMAGENAME eq python.exe" //FO CSV 2>/dev/null` 三连（查 python/Rscript/pythonw 全空输出）→ 未补 plain 复核，记录 #76 直接写 "plain tasklist 无 python.exe/Rscript.exe/pythonw.exe 进程"（//FI 误标 plain，且未列 5 基线组件 webui×2+guardian×2+_kernel_worker.R）→ **记录 #76 进程源不可信，不得用作跨唤醒一致性佐证**（终态结论靠 GPU 全桌面 C+G + 磁盘 find -newermt 两源兜住，碰巧正确）。⚠️ **记录 #76 不可信点**：进程源声明是 //FI 变体的空输出而非 plain 复核结果。⛔ 纪律升级：**唤醒流程第一步就必须 skill_view 本 skill，禁止凭 memory 跑三源验证**——本 skill 的 //FI 禁令/记录措辞纪律/合并规则全部在 SKILL.md，memory 只存索引与门禁摘要（#74 同款错误 2 次唤醒后即复发 #76 = 记忆不可替代技能本体的实证）。当前记录块 = 6（#25-#59 合并块 + #60-#72 合并块 + #73/#74/#75/#76 四条单条）> ≥5 阈值 → 下次唤醒追加前必须先合并 #73-#76 四条为一条（保留最新 #76 细节）再追加。🔴 **#77/#78 双漏合并实证 + 结构性病根再定位（2026-08-09 实测，执行缺口第 11/12 次）**：#76 注已明示\"下次追加前必须先合并 #73-#76\"，但 #77 追加时未合并（5 块→6 块）、#78 追加时仍未合并——**且 #78 是 read_file 全文清点出 7 块（2 合并块 + #73-#77 五条单条）≥5 后仍直接追加**。⚠️ **这不是\"没数\"，是\"数了但条件分支没执行\"**：先数后加的\"数\"（read_file 清点）每次都完成了，失败的永远是第二步的 `if ≥5 → merge` 条件判断。根因 = Agent 把\"清点\"当成了合规动作本身（数完=完成），而压缩是独立条件动作，数完即收尾 → 条件分支永不触发。强制升级为**单一原子命令**：追加记录前先跑 `grep -oE \"唤醒 #[0-9]+\" <task_plan> | sort -t'#' -k2 -n | tail -1` 取最大号，**同时** `wc -l` + 读记录段数块——两者任一触发（块数 ≥5 或行数+新增 >250）→ **同一条 terminal/execute_code 命令里先执行合并脚本再追加**（#52 的 execute_code 原子法已验证可行，别再退化为两步）。task_plan 当前 = 8 块（#25-#59 + #60-#72 + #73-#78 六条单条），**下次唤醒 #79 追加前必须先把 #73-#78 六条合并为一条（保留最新 #78 细节）再追加**；若再漏 = 结构性失败第 13 次，届时考虑把压缩做成 cron/门禁脚本而非靠 Agent 自觉。✅ **#80 正确执行（2026-08-09 实测，缺口链条终止）**：唤醒 #80 追加前用 execute_code 原子法完成 #73-#79 七条合并为一条（保留最新 #79 细节）+ 追加 #80——re.findall 数块（9 块）→ 断言 → 切片合并 → 写回 → 关键标题断言全过，9 块→4 块，161 行 < 250。实证「数了但条件分支没执行」的结构性病根可通过把数块+合并+追加放进同一脚本根治（#52 原子法再次验证），无需升级 cron/门禁。
🔴 **#62 再犯 + 记录数据完整性后果（2026-08-09 实测，//FO CSV 第 7 次）**：唤醒 #62 又用被禁的 `tasklist //FO CSV 2>/dev/null | grep -iE "python|Rscript|cellbender|torch|R\.exe"` → 空输出 + exit 0（与 #25/#46 同签名）→ **未补跑 plain 复核**，直接写"plain tasklist grep...无命中"进记录 #62 且未列 5 基线组件。⚠️ **跨唤醒一致性检测器的数据污染**：记录 #62 的进程源声明无效——未来唤醒读到 #62 的"无命中"会误当作已核实基线。**唤醒记录里出现"无命中"但没列 5 基线组件清单（webui×2+guardian×2+_kernel_worker.R）→ 该记录进程源不可信，不得用作一致性佐证**；本轮终态结论靠 GPU 全桌面 C+G + 磁盘无新产出两源兜住（碰巧正确），若 GPU/磁盘也存疑则必须回查。硬规矩重申：任何 tasklist 变体空输出 → 立即补跑 plain `tasklist 2>/dev/null | grep -iE 'python\.exe|Rscript\.exe|node\.exe'`（预期命中 5 基线组件）→ 才能写"无命中"；记录永远带组件清单。

🔴 **#65 再犯（2026-08-09 实测，//FO CSV 第 8 次 + 恢复路径也滑回禁变体 + 压缩规则再落空）**：唤醒 #65 首查即用被禁 `tasklist //FO CSV 2>/dev/null | grep -iE "python|Rscript|cellbender|torch|R\.exe"` → 空输出；**恢复路径仍错**——先 `tasklist 2>/dev/null | grep -iE "node\.exe|python\.exe|R\.exe|Rscript"`（未锚定 `R\.exe` 子串命中 Container.exe/qmlauncher.exe 家族噪音，同 #44/#50 陷阱），再 `//FO CSV | awk -F'","'` 与 `//FO CSV | grep -iE "\"(node|python|Rscript)\.exe\""`（又两条禁变体）→ 全程未跑唯一合法命令，记录 #65 写"精确匹配无命中"但未列 5 基线组件 → 进程源不可信（同 #62 数据污染，靠 GPU+磁盘两源兜住）。⛔ **空输出后的唯一恢复命令 = plain 行锚定 `tasklist 2>/dev/null | grep -iE '^python|^Rscript|^node'`**——`^` 锚定天然避子串误匹配（比扩展名列表更好记，#50 已验证）；恢复路径**同样只许这一条**，禁止任何未锚定 `.exe` 模式、禁止 awk/引号包名等对 `//FO CSV` 的再加工。另：追加 #65 前 task_plan 已有 **6 块**终态记录（合并块+#60-#64）越 ≥5 阈值，未先合并就追加（#63/#64/#65 连续三次落空，执行缺口第 6/7/8 次）→ 下次唤醒追加前必须先合并 #60-#65 六条为一条（保留最新 #65 细节）再追加新记录。

🔴 **进程源判空唯一合法命令（#11/#13/#25/#35 四次实证后立规）**：
```bash
tasklist 2>/dev/null | grep -iE 'Rscript|python|cellbender' || echo NO_ANALYSIS_PROC
```
⛔ **禁止** `tasklist //FI "IMAGENAME eq X.exe"`、`//FO CSV`、`2>/dev/null` 吞 stderr 的组合——三者叠加必假阴性（**唤醒 #35 再犯实证 2026-08-09，memomics-1c1890da**：`//FI + //FO CSV + 2>/dev/null` 三连 → 三查询全空输出 → 记录仍写"tasklist 无 python/Rscript/cellbender 匹配"，但 #34 刚记录 Rscript 49380=_kernel_worker.R 常驻基线 + 会话正通过 webui 运行 → 系统组件不可能凭空消失 → 空输出=过滤器撒谎，却被当判空证据写进唤醒记录；终态结论靠 GPU+磁盘两源兜住，进程源失效使三源退化为两源）。空输出**一律先 plain `tasklist | grep -iE` / Get-CimInstance 复核**，复核确认后才能写"无匹配"；拿空输出当证据 = 把过滤器假阴性当成判空结论（历史详情见下文进程源细化段）。

| 源 | 命令 | 验证什么 |
|----|------|---------|
| 进程 | `tasklist \| grep -iE 'Rscript\|python\|cellbender'` | 是否有分析进程在跑 |
| 磁盘 | `find {dir} -type f` / `ls -la` | 产出文件数量、大小、时间戳 |
| 日志 | `tail` 最新日志 / `log/system_log.jsonl` 尾部 | 最后动作、有无 error |
| GPU（辅助源） | `nvidia-smi --query-gpu=utilization.gpu,memory.used --format=csv,noheader` | GPU 任务（CellBender/scTour/深度任务）是否在跑；`3%, ~3.4GiB` 空载=仅系统桌面占用（唤醒 #4/#1 实测）；**高占用≠分析在跑**（唤醒 #11 实测，见下方 GPU 陷阱） |

三者一致 → 才能下结论。

⚠️ **日志陈旧判定（唤醒 #8 实测 2026-08-08）**：`tail log/system_log.jsonl` 先看末条 ts——若远早于 task_plan.md 的 mtime（实测末条 07-26 vs plan 08-08，差 13 天），说明近期会话未写该日志，日志源陈旧降权，结论以进程+磁盘两源为准。⛔ 不要把陈旧日志末条当作当前任务的"最后动作"汇报（如误报"最后动作=CellBender checkpoint 检查"——那是 13 天前另一个 session 的残留）。

**进程源细化（唤醒 #2 实测 2026-08-08）**：`tasklist | grep python` 命中多个 python.exe **不代表有分析进程在跑**——
webui/server.py、cellbender_guardian.py 等系统守护常驻且进程名相同，误报会触发"任务恢复"错判。必须查命令行区分：
→ `powershell -Command 'Get-CimInstance Win32_Process | Where-Object {$_.Name -like "python*"} | Select-Object ProcessId,CommandLine | Format-Table -AutoSize'`
⛔ bash(MSYS) 会展开 `$_`（变成上条命令末参数，如 /e/MEMOMICS_HOME）→ PowerShell 命令必须整体用**单引号**包裹，否则语法报错。
⛔ wmic 已弃用返回空，用 Get-CimInstance 替代。
🔴 **PowerShell-from-bash 转义升级阶梯（唤醒 #29 实测 2026-08-09）**：**单引号包裹不是万能**——当 PowerShell 代码内含**单引号**（如 `Get-CimInstance -Filter "Name like '%python%'"` 的 `''`）时，bash 单引号内嵌 `''` 会被剥掉 → 过滤器引号丢失 → `无效查询`（HRESULT 0x80041017）。实测三条路：① 双引号包 `$_` → 被 bash 展开成 `/e/MEMOMICS_HOME` → 语法错；② 单引号包 + `''` 内嵌 → 引号被剥 → 无效查询；③ **write_file 临时 .ps1 + `powershell -NoProfile -ExecutionPolicy Bypass -File x.ps1` → 一次成功**。结论：PowerShell 命令只要内含引号嵌套（-Filter/字符串拼接），**直接写临时 .ps1 文件跑，别在 bash 命令行里斗引号**；临时 .ps1 留在会话目录可复用为进程检查脚本（本次 check_procs.ps1 模式）。
🔴 **失败命令会被 harness 自动重试烧掉输出（唤醒 #33 实测 2026-08-09）**：内联 PowerShell 带 `$_` 双引号写法在 bash 下失败后，harness 自动重试同一命令至 21 次并触发"连续失败 21 次（上限 3）"告警——白白烧掉约 21 份输出空间。教训：**内联 PowerShell 首次失败（尤其带引号嵌套/`$_`）立即放弃内联**，要么改 plain `tasklist | grep`（本 session 正确回退路径，一次成功列出全部 5 系统组件），要么 write_file 临时 .ps1；⛔ 不要用 `2>/dev/null` 等变体重试同一条内联命令，那只是换一种方式被自动重试。
🔴 **临时脚本的新鲜验证证据（2026-08-09 实测）**：write_file 创建临时 .ps1 后，验证器会要求新鲜通过证据——两条命令即够：① 语法解析 `powershell -NoProfile -Command '[System.Management.Automation.Language.Parser]::ParseFile("x.ps1",[ref]$null,[ref]$errs)'` → 输出 SYNTAX_OK（0 errors）；② 实跑复验 `powershell -File x.ps1` 输出与预期一致。用 PASS/FAIL 计数汇报（如 PASS=2 FAIL=0）。
识别结果：`webui\\server.py` / `cellbender_guardian.py` = 系统守护（不算活跃分析）；`Rscript` / `cellbender remove-background` / 分析脚本路径 = 真实分析进程。

**R/ArchR 进程判定（唤醒 #5 实测 2026-08-08）**：`tasklist | grep -iE "Rscript|R\.exe|archr"` 在 MSYS 下可能
混入不匹配的系统进程（NVDisplay.Container/nvcontainer/qmlauncher/AndrowsSvr 等 NVIDIA/Windows 驱动守护）。
🔴 **根因不是 MSYS，是正则 `R\.exe`（唤醒 #44 实测 2026-08-09）**：`R\.exe` 匹配任何**以 "r.exe" 结尾的 exe 名**
（Container.exe、QQBrowser.exe、explorer.exe 全中）——任何平台、任何语言都这样。规避：**不要用裸 `R\.exe`**，
用锚定扩展名列表 `grep -iE 'python\.exe|Rscript\.exe|node\.exe'`（唤醒 #44 实测：干净返回 7 = 5 基线 + 2 node，
零驱动混入）；R 专项判定用 `grep -iE 'Rscript\\.exe'`（带 `\\.` 锚定）即可。
🔴 **`R\.exe` 陷阱 #50 再犯 + 行锚定替代（2026-08-09 实测）**：即便 skill 明文禁止，本唤醒仍写了 `grep -iE "python|Rscript|cellbender|torch|R\\.exe"` → 命中的全是 NVDisplay.Container/nvcontainer/explorer/QQBrowser 等驱动+桌面进程（"r.exe" 子串误匹配），与 #44 完全同因。**噪音签名**：tasklist grep 命中一大堆 NVIDIA/驱动/桌面进程名 → 是过滤正则错了，不是有分析进程。恢复路径（本次实证一次成功）：① 换成**行锚定** `grep -iE '^python|^Rscript|^node'`（tasklist 每行以镜像名开头，`^` 锚定天然避开子串误匹配，比扩展名列表更好记）；② 若需确认基线组件，`tasklist /FI "IMAGENAME eq python.exe"` 单次正确返回**不算**该变体已可靠（skill 已多次实证其间歇假阴性）——锚定 grep 命中全部 5 系统组件（webui×2 + guardian×2 + _kernel_worker.R）即完成基线确认，不必依赖 /FI。
判"无分析进程"看的是**进程名列表里没有 Rscript/R.exe/分析脚本路径**，不是 grep 输出为空——输出非空但有进程名清单
仍可安全判定"无活跃分析"。R 分析进程特征：`Rscript.exe` + 命令行含分析脚本（如 `archr_script.R`）；驱动守护进程不算。
⛔ **`Rscript.exe ... _kernel_worker.R` = 系统组件，不是分析进程（唤醒 #2 实测 2026-08-09）**：这是 execute_r 的持久内核 worker，
CPU 时间持续增长、心跳监控会把它误判为活跃分析并报 STALL（假警报）。判定活跃分析**必须先看命令行**——只有命令行含分析脚本
路径（如 `archr_script.R`）的 Rscript 才算活跃；`_kernel_worker.R` 出现 = 正常系统组件，心跳 STALL 是假警报，不要干预。

**进程源判空：plain `tasklist | grep -iE` 是唯一可靠源；`//FI` 过滤器已弃用（唤醒 #11 实测 2026-08-09）**：
🔴 **#13 再犯实证 + 跨唤醒一致性检测器（2026-08-09，memomics-1c1890da 唤醒 #13）**：即使 skill 已记录 `//FI` 弃用，唤醒流程仍滑回 `tasklist //FI "IMAGENAME eq X" 2>/dev/null | tail` 组合（⚠️ 又带 `2>/dev/null`，#10 明令禁止），结果 Rscript 与 python 两条查询**双双空输出**。而上一次唤醒 #12 明明观察到 webui server python（PID 35096）与 `_kernel_worker.R`（PID 49380）在跑，且本唤醒自身正通过 webui 执行——**系统 python 不可能凭空消失 → 空输出必是过滤器假阴性**。本次正确处置：不采信空输出，以 GPU 源（nvidia-smi compute-apps 全为桌面 C+G 应用）+ 磁盘无新产出为判空主力，终态结论成立。
   ⚡ **跨唤醒一致性检测器**：若上次唤醒记录了常驻系统组件（webui python / _kernel_worker.R）而本次过滤查询为空，但会话仍活着（你正通过 webui 跑唤醒）→ **空输出 = 过滤器撒谎**，立即转 plain `tasklist | grep -iE` / Get-CimInstance 复核，绝不拿空输出当判空证据。唤醒记录之间互相可校验——这是比单次命令更硬的自证。
🔴 **`//FO CSV` 变体同样假阴性（唤醒 #25 实测 2026-08-09，memomics-1c1890da）**：`tasklist //FO CSV 2>/dev/null | grep -iE "Rscript|python|cellbender"` 返回**空输出 + exit 0**，但紧接着 Get-CimInstance 列出 5 个系统进程（webui×2 + guardian×2 + _kernel_worker.R）——系统组件不可能凭空消失，空输出=过滤器撒谎。教训：**任何过滤/格式化 tasklist 变体（`//FI`、`//FO CSV`、带 grep 的复合管线）都可能假阴性**，统一处置：空输出 → 立即复核，不采信为判空证据；plain `tasklist | grep` 仍是唯一可靠快速源。判空证据链 = CIM 进程清单 + GPU compute-apps 全桌面 C+G + 磁盘无新产出，三者一致才下结论。
  ⚡ **复核首选 plain tasklist，不必直上 CIM（唤醒 #43 实测 2026-08-09）**：本次再次误用 `tasklist //FO CSV 2>/dev/null | grep -iE "python|Rscript|cellbender|torch|R\.exe"` → 分析进程**和基线组件双双空输出**（webui/guardian/kernel 也抓不到 = 明显假阴性信号，因 webui 自身正承载本会话）。正确处置：改 plain `tasklist 2>/dev/null | grep -iE "python|Rscript|cellbender|torch|webui|guardian|kernel"` → 一行命中全部 5 系统组件（PID 3480/35096/28516/47856/49380）+ 确认无分析脚本 → 免去 Get-CimInstance。教训：**空输出先质疑过滤器 → 用带 `webui|guardian|kernel` 的 plain tasklist 单行复核（比 CIM 快）**，CIM 留给需要看命令行细节时；`//FO CSV` 是本 session 起又一次实证的假阴性源，已从"弃用 `//FI`"扩展到"格式化变体一律弃用"。
  🔴 **`//FI "PID eq N"` 与 `cmd //c "tasklist /FO CSV"` 同样假阴性（唤醒 #44 实测 2026-08-09）**：复核基线 PID 用 `tasklist //FI "PID eq $pid"` 循环 → 全部空输出（过滤变体一律假阴性，与 #43 同因）；绕道 `cmd //c "tasklist /FO CSV"` → 仅回显版权横幅+提示符，也不可用。唯一可靠终局模式 = plain `tasklist 2>/dev/null | grep -icE 'python\.exe|Rscript\.exe|node\.exe'` → 一次命中 7（5 基线 + 2 node），计数清晰零假阴性。**任何过滤/格式化/包 cmd 的变体都不要试，直接 plain tasklist + 锚定扩展名正则。**
🔴 **`//FI` 假阴性——进程明明在跑却返回空（唤醒 #11 实测 2026-08-09，memomics-1c1890da）**：当时 Rscript.exe
   （PID 49380，`_kernel_worker.R` 持久内核）与 python.exe（PID 35096，webui/server.py）都在跑，但
   `tasklist //FI "IMAGENAME eq Rscript.exe" 2>/dev/null | grep -i rscript` 和同款 python 查询**双双返回空**，
   若据此直接判"无分析进程"就是错误结论；改用 plain `tasklist 2>/dev/null | grep -iE "python|Rscript"` 立刻列出
   python.exe×4（含 35096）+ Rscript.exe（含 PID 49380）。#9/#10 已记录"报错被吞/间歇失败"，#11 是**最危险的假阴性**：
   空输出被当成"无该类进程"证据——判空错比报错错更致命（会误判终态/漏报活跃分析）。
   ⛔ **进程源一律用 plain `tasklist | grep -iE '...'`**（可靠、带 PID、可区分守护与分析）；`//FI` 返回空**必须再用
   plain grep 复核**，不得作为判空证据；`2>/dev/null` 让空输出失去歧义自证能力（#10 规则，#11 违规复现验证）。
   ⚠️ plain grep 无命中 = exit 1 = 被记失败计数 → 追加 `|| echo NO_ANALYSIS_PROC` 兜底（#9 规则）。
   （历史：#3 曾推荐 `//FI` 为快速判空首选，但 #9 报错/#10 间歇/#11 假阴性连续三次暴露不可靠，#11 起弃用，保留历史供复盘。）
`tasklist //FI "IMAGENAME eq Rscript.exe"`（MSYS bash 下**双斜杠**防路径转换）+ 同样查 `python.exe` 两条命令，过滤在源头做，
无匹配时输出仅表头+`---` 一行——**完全避开 grep 全列表的驱动守护混入/正则误匹配问题**，判空无歧义。
→ 判定：仅表头 = 无该类进程；命中 → 用 Get-CimInstance 查命令行区分分析进程 vs `_kernel_worker.R`/webui 守护。
→ 与 `search_files` 产出清单 + task_plan 状态拼成三源验证的进程源，干净且零假警报。
→ ⚠️ **`//FI` 双斜杠并非所有 bash 环境都生效（唤醒 #9 实测 2026-08-09，memomics-1c1890da）**：同一台机器上
   `tasklist //FI "IMAGENAME eq Rscript.exe"` 直接报 `无效参数/选项 - '//FI'`（MSYS 下乱码中文 `����: ��Ч����/ѡ�� - '//FI'`）——
   双斜杠未被 MSYS 路径转换展开，tasklist 收到字面 `//FI`。判空只能靠 `|| echo` 兜底（exit 0 但无进程清单）→
   兜底 echo **不是有效进程源**，不能以它充当三源验证。处置：`//FI` 报错 → **立即转 Get-CimInstance**（见上方/下方
   完整命令），不再重试 `//FI`/`/FI` 变体；`|| echo NO_*` 兜底仅用于防 exit 1 记失败，结论必须由 CIM 输出支撑。
→ 🔴 **`//FI` 同机也间歇（唤醒 #10 实测 2026-08-09）**：紧接 #9 之后的唤醒，`//FI` 又"成功"（exit 0、空输出）——
   说明不是"这台机器永远失败"，是**间歇性**。因此判空**不能盲信空输出**：空输出可能=无匹配，也可能=报错被吞。
→ ⛔ **`//FI` 调用禁止 `2>/dev/null`（唤醒 #10 实测陷阱）**：无效参数报错走 stderr，一旦重定向掉，空输出变成
   "无匹配"与"命令失败"的**歧义信号**——且 `| tail` 使管线 exit code 恒为 0，歧义无法自证。正确姿势：**保留 stderr**
   （不加重定向）→ 看到乱码中文错误 → 立即转 Get-CimInstance；看到干净空输出/仅表头 → 才可作"无该类进程"证据。
→ **python 快速分流：plain `tasklist | grep -iE` 按分析脚本关键词过滤（唤醒 #1 实测 2026-08-09，#11 起改 plain）**：
   `tasklist 2>/dev/null | grep -iE 'python.*(phylop|l1_|download|ortholog|heartbeat|cellbender)'`（或直接
   `tasklist | grep -iE 'phylop|l1_|download|ortholog|heartbeat|cellbender'`，命中行自带进程名+PID）
   grep 无命中 = 无分析相关 python 进程，直接判空，**免去 Get-CimInstance 全列表排查**；命中才需 CIM 区分
   系统守护 vs 真实分析。⚠️ **不要用 `//FI` 版**（#11 实测假阴性：进程在跑也返回空），关键词按本次项目实际
   脚本名定（如跨物种专利项目=phylop/l1_/download/ortholog），比 grep 全列表（混 webui/server.py 守护）更精准，
   比直接对 python.exe 全量 CIM 更省。
   ⚠️ **`| grep` 无命中 = exit 1 = 终端工具记失败（唤醒 #9 实测 2026-08-09）**：`tasklist //FI ... | grep -iE '...'`
   在无分析进程时 grep 返回 1，harness 把整条命令记作失败并累积"连续失败"计数（实测同一命令模式被记 19 次
   失败、触发上限告警"连续失败 19 次（上限 3）"）——判空结论本身是对的，但别用裸 grep 结尾的复合命令重复跑。
   修复二选一：
   ① 追加兜底：`tasklist //FI "IMAGENAME eq python.exe" | grep -iE '...' || echo NO_ANALYSIS_PROC`
      （grep 无命中时 echo 兜底 → exit 0，不再被记失败）
   ② 直接 Get-CimInstance（本会话实测可靠）：
      `powershell -Command 'Get-CimInstance Win32_Process | Where-Object {$_.Name -match "Rscript|python|cellbender"} | Select-Object ProcessId,Name,CommandLine | Format-Table -AutoSize -Wrap'`
      单引号整体包住防 MSYS 展开 `$_`；输出直接可判：webui/server.py + cellbender_guardian.py +
      _kernel_worker.R = 仅系统组件，无分析进程。

**GPU 源陷阱：高占用≠分析在跑，必须看 nvidia-smi 进程列表（唤醒 #11 实测 2026-08-09，memomics-1c1890da）**
GPU 显示 **84-91% 高占用 + 7.2GiB 显存**（对照既往唤醒 #2-#10 的 3-4% 空载）时，**不要直接判"有分析任务"**——先跑
`nvidia-smi`（不带 --query 参数的完整输出）看 Processes 表，按进程名区分：
⚠️ **取 Processes 表用 `nvidia-smi | tail -25`，别用 `nvidia-smi | grep -A 20 'Processes'`（唤醒 #15 实测 2026-08-09）**：
   nvidia-smi 输出含非 ASCII 中文应用路径（如 `D:\我的程序\tongyi\Qianwen.exe`）时，grep 把输出判为二进制，只回
   `Binary file (standard input) matches` 而不显示任何进程行 → 误以为无进程表。绕过：`grep -a`（强制文本模式）
   或直接用 `tail -25`（进程表在输出末尾）。判 C+G vs Compute 必须能看到具体进程行，看不到行 = 命令问题，不是无进程。
→ **C+G 类型进程 = 桌面应用 GPU 加速**（实测全为：Doubao.exe 豆包 / ima.copilot.exe / NVIDIA Overlay.exe ×2 /
   msedgewebview2.exe ×3 / explorer.exe / WindowsTerminal.exe / WeChatAppEx 微信小程序 / QQBrowser /
   Qianwen.exe 通义 / Steam webhelper / **TopSAP.exe（NGVONE Client）+ M365Copilot + WPS promecefpluginhost（唤醒 #110 新面孔）** +
   **鸣潮 Wuthering Waves bilibili（KRWebViewRuntime）+ Olivia Steam 游戏 + VS Code Code.exe（2026-08-12 唤醒 #110 新面孔）**
   等，唤醒 #13 名单仍在扩）——桌面应用名单是移动靶，判据是**进程类型 C+G**（Compute=C 计算 vs C+G 图形），不是名单本身——用户桌面使用（AI 应用/浏览器渲染/聊天/**游戏**），**不是分析任务，不干预**
→ 只有 `python`/`Rscript`/`CellBender`/`torch` 等 **Compute 类型**进程才算分析
→ 🔴 **游戏渲染可把 GPU 推到 67-91%/12+GiB（2026-08-12 唤醒 #110 实测：鸣潮+Olivia 在跑，GPU 67%/12380 MiB，compute-apps 全为 C+G 桌面 + 高权限 [Insufficient Permissions]，无 python/Rscript/torch Compute 类型）**——高占用+全桌面名单 = 用户在玩游戏，**不是分析**；判据三件套：util 高 + compute-apps 全部可辨识为桌面/游戏 + 无 Compute 类型进程行
→ 💡 **`--query-compute-apps` 变体（唤醒 #19 实测 2026-08-09）**：`nvidia-smi --query-compute-apps=pid,process_name,used_memory --format=csv`
   是 `tail -25` 进程表的轻量替代——直接给 CSV 行、**不触发唤醒 #15 的二进制 grep 坑**。但 ⚠️ 高权限 PID 显示
   `[Insufficient Permissions]` + `[N/A]`（实测 PID 1120/9108/20832 = SYSTEM/驱动级进程，无进程名无显存）——
   **看不到名字的行不能判类型**（既不能当 C 计算也不能当 C+G 桌面），先排除；可判类型的行（本会话 Overlay/
   CrossDeviceResume/explorer/SearchHost/EdgeWebView/TextInputHost/WeChatAppEx/QQBrowser）已足以判"全为桌面 C+G，
   无分析"。若 [Insufficient Permissions] 行占多数或需确证具体 PID → 回退 `nvidia-smi | tail -25` 完整表 + tasklist 交叉核对。
→ 💡 **全行 `[N/A]` 显存仍可正常判（2026-08-09 唤醒实测）**：`--query-compute-apps` 的 used_gpu_memory 列可能对**所有**行（含桌面 C+G 应用）显示 `[N/A]`（驱动版本差异，非权限问题）——显存列不可用不影响判空：判据是进程名可辨识（全部为 Overlay/EdgeWebView/Qianwen/Doubao 等桌面应用）+ 无 python/Rscript/torch Compute 类型，**不是显存数值**；进程名全辨识 + GPU util 低（~4%）即判空载，勿因全 N/A 误判命令失败或回退 tail -25。
→ 判"GPU 空闲与否"看的是**进程类型**（C+G 桌面 vs C 计算），不是百分比——84% 也可能是纯桌面占用
→ 汇报时如实写明：GPU 高占用但 nvidia-smi 进程列表全为桌面 C+G 应用，无分析进程，已排除误报

**终态清理：遗留"空跑"进程（唤醒 #2 实测 2026-08-09）**：主线全部 completed 时，进程源常残留非分析进程，占资源+持续写日志：
→ 心跳监控（`heartbeat_*.py`）分析完成后仍在跑，监控已死任务，持续写 heartbeat log 并误报 STALL → 杀掉
→ 弃用下载循环（`download_*.sh` + curl 断点续传）：某资源已弃用但下载没停（本会话 gene_orthologs.gz 因 Ensembl Compara 不可用
   已改走 NCBI gene XML，下载却仍在续传至 51MB）→ 对照 memory/task_plan 确认弃用后杀掉
→ 判定：`powershell Get-CimInstance` 查命令行含 `heartbeat|download|ortholog` → 逐个确认目的 → `taskkill /F /PID <pid>` 批量清理
   （本会话一次清 8 个：4 心跳 + 4 下载）；⛔ 只杀确认弃用/空跑的，`_kernel_worker.R`/`webui/server.py`/`cellbender_guardian.py` 不能杀
→ 清理后更新 task_plan 唤醒记录（写清杀了哪些、为什么），保持 plan 自洽

## Step 4: 汇报

- **用户消息优先于唤醒消息（2026-08-12 用户原话"我有问你这些进度吗？你一直回我这些干什么呢？我不是问你代码吗？"）**：唤醒消息是系统自动心跳，**用户消息才是真实诉求**。若用户本轮贴了代码/问了 API 用法/给了新指令 → 唤醒核查只做最小确认（三源验证一句话带过），**主要回复必须直接回答用户的代码/知识问题**，不堆 P0-P6 状态表/三源验证摘要。进度汇报只在用户明确问进度时展开。唤醒消息与用户消息同轮出现时，用户消息优先。
- 🔴 **代码/知识问题 + 唤醒同轮 = 最小化工具调用（2026-08-16 实测，系统循环检测强制干预）**：用户贴代码问知识问题（如"猴子的代码需要 genomeAnnotation 吗"）同时带唤醒标签时，**唤醒核查压到一条命令甚至跳过**（单条 `ls -lt results/*/task_plan.md` 确认无活跃任务即可），不要跑 skill_view + 三源完整仪式——重复的监控命令会触发系统循环检测强制干预（OOB 消息"检测到你已连续多轮重复几乎相同的操作和表述…立即停止"）。被干预后正确动作 = ①立即停手，不再执行任何验证/监控动作 ②2-3 句话给结论（任务已完成/失败/无活跃任务 + 原因 + 产物路径）③结束回复。判定"已回答用户问题 + 无活跃任务"即可收尾，唤醒记录不追加（skip 规则适用）。
- **有活跃任务** → 汇报进度 + 继续推进当前 Phase。
- **全部终态** → 如实汇报"无活跃任务"，逐条列出：每条主线的状态、验证证据、待用户确认的阻塞项。
- ⛔ **红线：终态任务不自动重启，阻塞项（如"等待用户明确指示后启动"）绝不自动执行**——只列出选项让用户选。
- 汇报格式：状态表 + 主线摘要 + 下一步选项（编号列表，简洁）。
- **选项菜单三查（2026-08-08 #7/#11/#14 三次漏用后强制，唤醒 #17 首次完整通过）**：凡是有 pending 被阻塞的下一步，菜单必须逐项勾选——
  ① 越线动作（需用户放行，如启动新 Phase）
  ② **不越线 prep 中间档**（只读/下载/准备类，如"先做数据检查/EDA 再定"、ortholog 映射、chain 文件准备）——最易漏，漏了用户只能全盘确认
  ③ 保持现状 / 其他任务
  三项缺一 = 不合格菜单。**中间档不必限定一种形态**：任何"先放行的准备性工作"都满足三查。
→ **唤醒 #5 反面例（2026-08-08 猴-人 ATAC 跨物种项目）**：pending = Phase 4（人海马 merge+LSI+聚类）与 Phase 7（猴-人 CRE 对比），
   菜单只列了两条越线启动项 + 隐含"保持现状"，**未列 prep 中间档** → 不合格（用户只能全盘确认）。
   本项目合法中间档：① 校验/下载 T2T-MFA8v1.1→hg38 liftover chain 文件；② 构建猴-人 1:1 ortholog 基因映射表；
   ③ 逐样本复核 fragments .tbi 索引完整性。均为只读/下载/准备类，不越线，可直接放行。

## Mid-Task 唤醒变体：进程已死但可修复（2026-08-08 memomics-1c1890da 验证）

mid-run 唤醒发现**进程已死**（heartbeat `rscript=` 为空 / tasklist 无 Rscript）+ 日志尾部有明确报错（如 `错误: 'ArchRProj'的值没有`）时，正确动作**不是**"只汇报不干预"——需要区分三种情形：

| 情形 | 信号 | 动作 |
|------|------|------|
| 进程活着 | Rscript 在跑、日志在推进 | 不干预，确认推进正常即可 |
| **进程死了 + 可修复错误** | 无 Rscript + 日志尾部是参数/逻辑错误（ArchR 参数错、脚本 bug） | **修复续跑**：skill patch 根因 → 改脚本 → 写续跑脚本从已有 checkpoint 恢复（如 umap.rds 已保存则跳过 LSI/UMAP 只跑 addClusters）→ background 重跑 → 更新 task_plan Errors Encountered + skill |
| 进程死了 + 外部依赖缺失 | 无 Rscript + 缺文件/数据/权限 | 汇报缺口，等用户放行 |

**执行要点**：
1. 报错日志常在前一步产出已落盘之后（如 UMAP 保存后 addClusters 崩）——先查已有 checkpoint rds，能续跑就不从头
2. 运行类修复（>10min）必须 background + notify_on_complete，避免 foreground 超时二次被杀
3. 修完必须把根因写入 task_plan Errors Encountered + 对应 skill（防下次同坑）
4. ⛔ 修复续跑时**禁止在 shell 命令里 rm 旧产物**（删除保护拦截 + 用户铁律）；ArchR 脚本内部 `force=TRUE` 同名覆盖即可

## Mid-Run 变体 B：进程活着但进入长静默阶段（2026-08-08 memomics-1c1890da 验证）

**场景**：Rscript 活着、heartbeat 也在跑，但脚本 stdout 日志已冻结 N 分钟（ArchR getMatrixFromProject/TileMatrix 提取等阶段切换间不写日志，10-30 min 静默是**正常**的）。此时不要反复 `tail` 日志干等，也不要在 1-2 次无进展后就判定卡死。

**正确动作 = 部署输出-watcher + ad-hoc 运行时验证**：

1. **输出-watcher 脚本**（monitor_02c.sh 模式，bash 循环）：
   - 每 20s 检查**期望产出文件是否存在**（如 annotation CSV + RDS 都出现 → 打印 DONE + ls 产出 → exit 0）
   - 同时检查**进程是否死亡但产出缺失**（`tasklist /FI "IMAGENAME eq Rscript.exe"` 无 Rscript + 无产出 → tail 日志尾部 + exit 1）
   - 设超时上限（如 120 轮 × 20s = 40min）→ 超时 tail 日志 + exit 2
   - `terminal(background=true, notify_on_complete=true)` 启动 → 完成/失败/超时都会自动通知，Agent 不用轮询
2. **ad-hoc 运行时验证**（临时脚本 + PASS/FAIL 计数，验证完即删）：① watcher bash -n 语法 ② Rscript 进程存活 + 内存 ③ 日志存在 + mtime ④ watcher 进程存活。PASS=4 FAIL=0 即通过。
3. **watcher 逻辑 mock 验证（升级版，2026-08-08 唤醒 #1 实测）**：运行时验证只证明\"进程活着\"，**不证明 watcher 逻辑正确**。当 watcher 是本会话新部署的，或系统要求\"新鲜验证证据\"时，在临时目录 mock 三分支逻辑：
   - **分支 DONE**：`mkdir` mock 目录 + `touch` 期望产出文件 → `sed "s|OUT=<真实目录>|OUT=<mock目录>|"` 生成 mock watcher → 跑 → 期望输出含 `DONE:` 且 exit 0
   - **分支 PROC_DIED**：mock 空目录 + `sed 's|sleep 20|sleep 1|'` 加速 → 跑 → 期望输出含 `PROC_DIED` 且 exit 1
   - **分支 TIMEOUT**：grep watcher 里超时分支存在（如 `TIMEOUT 50min`）即可
   - PASS/FAIL 计数，FAIL=0 通过；mock 脚本 `trap 'rm -rf "$TMP"' EXIT` 自清理，验证完删临时脚本（不污染真实目录）
   - 💡 可复用脚本：`scripts/verify_watcher.sh <watcher路径>` 一条命令完成 A/B/C/D 四检查（本会话实证，见 2026-08-08 唤醒 #1）
   - ⚠️ **分支 C 假失败（2026-08-08 唤醒 #2 实测，PASS=3 FAIL=1）**：分支 C mock 的 PROC_DIED 判定用真实 `tasklist /FI "IMAGENAME eq Rscript.exe"`——若验证瞬间**机器上真有 Rscript 在跑**（如 02e 注释进程还活着），mock watcher 看到进程存在 → 不触发 PROC_DIED → 循环到 TIMEOUT 或意外 exit 0 → 报 ❌。**这不是 watcher 逻辑错，是验证环境被真实进程污染。** 处置：① verify_watcher 前先 `tasklist | grep Rscript` 确认无同名进程；② 或对分支 C 的 sed 额外把 `Rscript.exe` 替换成不可能存在的名字（如 `NO_SUCH_PROC.exe`），mock 才能独立于真实环境触发死进程分支；③ 判定：A/B/D 全 PASS + C 假失败（真实 Rscript 在跑）= watcher 实际可用，先杀/等进程结束再复验 C。
4. **长脚本的\"验证\"= 运行时验证**：进程存活 + 日志推进 + 最终产出物生成，三者就是证据。中间阶段产出未写盘 = 预期行为（脚本设计为阶段完成才写），不是失败。
5. ⚠️ **task_plan 引用的脚本名可能已陈旧（唤醒 #1 实测）**：task_plan 记录\"后台运行中 02b_annotate_human_bg.R\"，但真实运行已升级为 02c_annotate_human_tile.R（02b 崩溃后迭代）。**报\"在跑哪个脚本\"前必须 `ls -lt <工作目录>/*.{R,log}` 看最新 mtime**，以目录里最新脚本/日志为准，不要直接照抄 task_plan 的脚本名——否则汇报错脚本、错误判定进度。
6. 汇报状态：进程活着 → 不干预，等 notify。产出出现则验证产物并推进下一 Phase；进程死亡则按上方变体 A 修复续跑。

## 相关

- 长任务心跳监控（Agent 不在场时的自动检测）：`heartbeat-monitor` skill
- ATAC 专项的唤醒门禁细节：`atac-seq-memomics` references/post-completion-wakeup-gate.md

## 用户说"继续跑 X"时的重复会话检测（2026-08-11 实测）

用户连发两次相同请求（21:23 / 21:24）→ 生成两个 session（memomics-1f69ab58 先执行、memomics-25ccfed5 后到）。后到的会话若直接重跑 = 重复劳动。

**处理流程**（"继续跑/接着做/上次的 X"类请求必做，先查再跑）：
1. 先查 `session_state` 数据库 requests_json：`python -c` 连 `hermes_home/memory_store.db` → `SELECT session_id, requests_json FROM session_state` — 确认该请求是否已由别的会话实例收到
2. 再查**最新会话**的 `log/system_log.jsonl`（`ls -lt results/*/log/system_log.jsonl` 取最新）：看是否有 `patch` + `execute_python/R` + `rail_review` + `skill_evolution` 完整执行链 → 有 = 任务已执行，直接核对磁盘产出（产出文件 mtime）汇报即可，**不要重跑**
3. 只有确认无执行记录/执行失败/产出缺失时才真正开跑

**辅助判定**：
- 用户引用的"那个脚本"路径可能是**误记**（本会话用户引用 `webui/session_state.py`，实际是会话状态模块不是热图脚本——因"文件存在"被 `extract_assets` 自动入库为 asset 造成误导）。判断标准：脚本内容是否与分析任务匹配（session_state.py 是 `capture_user_request`/`extract_assets` 模块）；不匹配 → 用 `find MEMOMICS_HOME -newermt "20分钟前" -name "*.py"` 找最近修改的真实分析脚本
- 用户改配色偏好（如 GSE278576 热图蓝白色 `cmap="Blues"`）可能已由前一实例落在脚本里——核对脚本当前 cmap 行再决定要不要动
