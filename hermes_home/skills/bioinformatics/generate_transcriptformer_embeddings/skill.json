{
  "id": "generate_transcriptformer_embeddings",
  "name": "Generate Transcriptformer Embeddings",
  "category": "genomics",
  "language": "Python",
  "source": "Biomni",
  "description": "Generate Transcriptformer embeddings for single-cell RNA-seq data. This function downloads model checkpoints, prepares the AnnData object with required fields (ensembl_id, raw counts, assay metadata),",
  "detailed_description": "Generate Transcriptformer embeddings for single-cell RNA-seq data. This function downloads model checkpoints, prepares the AnnData object with required fields (ensembl_id, raw counts, assay metadata), and runs inference to generate cell or gene embeddings. Transcriptformer is a transformer-based model that can learn rich representations of single-cell gene expression data. The function automatically handles Ensembl ID pattern detection, model downloading, data preprocessing, and creates missing assay metadata columns with 'unknown' values if needed.",
  "when_to_use": "When you need generate transcriptformer embeddings analysis",
  "parameters": {
    "adata_filename": "[Required] Name of the input AnnData file (.h5ad format) (str)",
    "data_dir": "[Required] Directory containing the input data file (str)",
    "output_filename": "[Optional] Name of the output embeddings file. If None, will use input filename with '_transcriptformer_embeddings' suffix",
    "model_type": "[Optional] Type of transcriptformer model to download and use. Options: 'tf-sapiens', 'tf-exemplar', 'tf-metazoa' (default: tf-sapiens)",
    "checkpoint_path": "[Optional] Path to the transcriptformer checkpoint directory. If None, will use './checkpoints/{model_type}'",
    "batch_size": "[Optional] Batch size for inference (default: 8)",
    "precision": "[Optional] Precision for inference. Options: '16-mixed', '32' (default: 16-mixed)",
    "clip_counts": "[Optional] Maximum count value to clip to (default: 30)",
    "embedding_layer_index": "[Optional] Which layer to extract embeddings from (-1 for last layer) (default: -1)",
    "num_gpus": "[Optional] Number of GPUs to use (default: 1)",
    "n_data_workers": "[Optional] Number of data loading workers (default: 0)",
    "gene_col_name": "[Optional] Column name in AnnData.var containing gene identifiers (default: ensembl_id)",
    "pretrained_embedding": "[Optional] Path to pretrained embeddings for out-of-distribution species",
    "filter_to_vocabs": "[Optional] Whether to filter genes to only those in the vocabulary (default: True)",
    "use_raw": "[Optional] Whether to use raw counts from AnnData.raw.X (True), adata.X (False), or auto-detect (None/auto) (default: None)",
    "emb_type": "[Optional] Type of embeddings to extract: 'cell' for mean-pooled cell embeddings or 'cge' for contextual gene embeddings (default: cell)",
    "remove_duplicate_genes": "[Optional] Remove duplicate genes if found instead of raising an error (default: False)",
    "oom_dataloader": "[Optional] Use map-style out-of-memory DataLoader (DistributedSampler-friendly) (default: False)"
  },
  "proven_params": [],
  "proven_script": null,
  "error_count": 0,
  "success_count": 0
}